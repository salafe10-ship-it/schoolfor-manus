# تقرير تدقيق تكامل المستخدمين والصلاحيات مع شؤون العاملين

**التاريخ:** 13 سبتمبر 2026  
**النطاق:** `SchoolUsersPermissionsModule`، `HumanResourcesPortal`، دليل الأقسام والوظائف، سجل HR الكانوني، مسارات الهوية والصلاحيات في `server.ts`.

## الخلاصة التنفيذية

الوحدتان تعملان على أساس API موثوق وRBAC مركزي، وقد عولج أثناء هذه المراجعة أخطر خلل تشغيلي: اختلاف مصدر دليل الوظائف. كانت شاشة المستخدمين تقرأ من قناة Supabase مختلفة عن سجل HR الذي يتحقق منه الخادم عند الحفظ، لذلك كان يمكن عرض وظيفة للمسؤول ثم رفضها عند إنشاء أو تعديل المستخدم.

تم إصلاح هذه النقطة في `server.ts`: مسار `/api/school/job-catalog` يقرأ الآن الأقسام والوظائف والموظفين من نفس `public.hr_database` الذي تستخدمه عمليات HR والتحقق من إنشاء المستخدم وتعديله. كما أضيف اختبار عقد يمنع إعادة إدخال مصدر بديل.

## أدلة الاختبار المرئي

تم اختبار الواجهة المنشورة بصريًا عبر المتصفح ضمن مدرسة «مدارس الأسرة الحديثة» والفرع الرئيسي:

| المسار | النتيجة المرئية |
|---|---|
| لوحة المدرسة ← المستخدمون والصلاحيات | فتحت الوحدة بنجاح، وبعد اكتمال التحميل ظهر 6 مستخدمين و4 أدوار و133 صلاحية. ظهرت حالة تحميل مؤقتة قبل اكتمال القراءة، وكان زر «مستخدم جديد» قابلًا للنقر خلالها. |
| الأدوار المعتمدة | ظهرت 4 قوالب أدوار مع صلاحياتها، والتبويب للعرض فقط كما هو موضح للمستخدم. |
| مصفوفة الصلاحيات | ظهرت 133 صلاحية عبر 25 وحدة، مع فصل الموروث، قرار المدرسة، والمنع المركزي. محرر القرار يعمل بصريًا داخل نافذة قابلة للتمرير. |
| محرر قرارات مستخدم | ظهرت صلاحيات HR وIdentity وFinancial وStudent بمصدرها وحالتها الفعالة. المنع المركزي ظهر مقفلًا. |
| لوحة المدرسة ← شؤون العاملين | فتحت البوابة، لكن سجل HR الكانوني في المدرسة المختبرة كان فارغًا: 0 موظفين، 0 أقسام، 0 وظائف. هذا يختلف عن دليل الوظائف الذي كان يظهر في وحدة الهوية قبل الإصلاح. |
| نموذج إضافة موظف | النموذج يفتح رغم خلو الأقسام والوظائف، والقسم والمسمى غير إلزاميين، بينما الرقم الوطني موصوف بأنه 12 خانة دون تحقق فعلي من 12 رقمًا. |

ملاحظة: ظهرت أثناء التنقل رسالة «السنة الدراسية الموثوقة غير صالحة أو غير محددة» في البوابة المنشورة. لم تمنع فتح شاشة HR، لكنها خلل بيئي/سياقي مستقل يحتاج معالجة في مصدر السنة الدراسية الموثوقة.

## ما تم إصلاحه في هذه الجولة

1. توحيد قراءة دليل الأقسام والوظائف مع سجل HR الكانوني في PostgreSQL.
2. منع اختلاف البيانات بين ما تعرضه شاشة الهوية وما تقبله عمليات إنشاء/تعديل المستخدم.
3. إرجاع قائمة الموظفين النشطين من نفس دليل HR في مسار دليل الهوية تمهيدًا لربط الحسابات بسجلات العاملين.
4. إضافة اختبار عقد يثبت أن مسار دليل الوظائف لا يستخدم قناة HR بديلة.
5. تمرير `Hr.Edit` و`Hr.Approve` و`Financial.Write` من صلاحيات الجلسة إلى بوابة HR، مع وضع عرض فقط وحراسة عمليات التعديل والاعتماد والصرف.
6. حماية الحذف وإنهاء العقود وجميع معالجات نماذج HR قبل تغيير الحالة المحلية عند غياب صلاحية التعديل.
7. استعادة آخر Snapshot مؤكد عند فشل أو تعارض حفظ HR، مع عرض خطأ وإجراء إعادة مزامنة.
8. تشديد نموذج الموظف: الرقم الوطني 12 رقمًا، والقسم والوظيفة وتاريخ المباشرة والراتب وفق قواعد واضحة، وتعطيل التعيين عند خلو الأدلة التنظيمية.

## النواقص ذات الأولوية

### P1 — ربط الحساب بسجل الموظف

حساب المدرسة يحتوي `job_id` و`job_title` و`department` كنصوص/مراجع جزئية، بينما سجل HR يحتوي `employee.id` و`jobId` و`departmentId`. لا يوجد `hr_employee_id` في `public.users` ولا حقل ربط في نموذج المستخدمين. النتيجة: يمكن إنشاء حساب لموظف دون معرفة هل له ملف HR، ويمكن أن تتكرر هوية الموظف بين الوحدتين.

**المتبقي:** إضافة مرجع `hr_employee_id` اختياري ومتحقق خادميًا، إظهار الموظفين النشطين في نموذج المستخدم، وإظهار حالة الحساب المرتبط داخل بطاقة الموظف في HR. لا يُسمح بحذف/أرشفة ملف HR إذا كان مرتبطًا بحساب دون مسار فك ارتباط موثق.

### P1 — صلاحيات واجهة HR غير مطابقة للصلاحيات الخادمية

الدخول إلى HR محكوم بـ`Hr.View`. تم تمرير القدرات الموثوقة إلى المكونات، وإضافة وضع العرض فقط، وحراسة عمليات التعديل والحذف والحضور وسائر النماذج. كما أصبح زر اعتماد/صرف المسير يعكس الصلاحية المناسبة بصريًا.

**المتبقي:** فصل مسار اعتماد المسير خادميًا إلى `Hr.Approve` بدل الاعتماد الانتقالي على `Hr.Write`، بعد مراجعة قوالب الأدوار المنشورة حتى لا ينقطع وصول الأدوار الحالية.

### P2 — التراجع عند تعارض/فشل حفظ Snapshot HR — تم الإصلاح

الحفظ الكانوني يستخدم `expectedVersion`. عند تعارض الإصدار أو رفض البيانات تستعيد الواجهة آخر Snapshot مؤكدًا، وتعرض سبب الخطأ وزر إعادة المزامنة، فلا تبقى بيانات غير محفوظة معروضة كأنها حقيقة.

**المتبقي:** يمكن لاحقًا استبدال إعادة تحميل الصفحة بإعادة جلب داخلية تحفظ تبويب المستخدم وحالته البصرية.

### P2 — نموذج الموظف والتحقق — تم تشديده في الواجهة

أصبح القسم والمسمى الوظيفي إلزاميين في النموذج، مع تحقق من 12 رقمًا للرقم الوطني، وتاريخ مباشرة العمل، والراتب غير السالب. البريد الإلكتروني المهني اختياري دائمًا، ويُطبق تحقق الصيغة فقط عند إدخاله. كما يتعطل بدء التعيين عندما لا توجد أدلة تنظيمية.

**المتبقي:** نقل قواعد الرقم الوطني وبعض قواعد الحقول إلى `validateHrSnapshotData` كتحقق خادمي صريح، وإضافة تحقق IBAN حسب البلد وسياسة المدرسة.

### P2 — دورة اعتماد HR والصرف المالي تحتاج فصلًا أوضح

الواجهة تعرض الاعتماد والصرف ضمن HR، بينما الخادم يستخدم `Hr.Write` لاعتماد المسير وتوقيع العقد و`Financial.Write` للصرف. هذا صحيح جزئيًا لكنه غير ممثل بوضوح في الواجهة، وبعض تغييرات الحالة العامة تمر عبر snapshot HR واحد.

**الإصلاح المقترح:** تعريف مصفوفة أفعال صريحة: قراءة، تحرير، اعتماد HR، صرف مالي، تصدير. حراسة كل زر بالقدرة المناسبة، ومنع تعديل السجلات المعتمدة إلا عبر مسار تصحيح/إلغاء موثق.

### P2 — سجل التدقيق التفصيلي في HR

السجل الكانوني يسجل snapshot hash و`changedCollections`، بينما `EmployeesTab` ينشئ `auditLogs` نصية داخل الحالة من المتصفح وباسم ثابت مثل «مدير الموارد البشرية». هذا مفيد للعرض، لكنه ليس مصدر تدقيق مؤسسيًا.

**الإصلاح المقترح:** جعل سجل التدقيق الخادمي هو المصدر الوحيد، وربط كل حدث بـactor وrequest/correlation id والنسخة السابقة واللاحقة، ثم عرض الأحداث في HR وIdentity من نفس المستودع.

### P2 — حالة المدرسة الجديدة والفارغة

إنشاء المدرسة يهيئ HR بسجل فارغ، وهذا سلوك آمن ومناسب لعدم اختلاق موظفين. لكن الواجهة لا تقدم مسار تهيئة واضحًا عند وجود 0 أقسام و0 وظائف، وتترك زر التعيين مفتوحًا.

**الإصلاح المقترح:** شاشة بداية فارغة واضحة: «ابدأ بتعريف قسم ثم وظيفة ثم موظف»، مع تعطيل إنشاء الموظف أو السماح به فقط مع اختيار «غير مرتبط» موثق. يجب أن تعرض وحدة المستخدمين نفس الحالة دون قوائم وظائف وهمية.

## التقييم الاحترافي

| المجال | التقييم الحالي | الحكم |
|---|---:|---|
| عزل المدرسة/المستأجر في API | قوي | النطاق مشتق من الهوية الموثوقة ولا يقبل من المتصفح. |
| RBAC المركزي ووظائف المدرسة | جيد بعد الإصلاح | مصدر دليل الوظائف أصبح موحدًا، مع بقاء حاجة ربط الموظف بالحساب. |
| التزامن والإصدارات | جيد جدًا | `expectedVersion` والحماية بعد اعتماد الرواتب/العقود موجودان، مع rollback واضح عند الفشل. |
| صلاحيات واجهة HR | جيد بعد الإصلاح | قدرات القراءة والتعديل والاعتماد والصرف تمرر من الجلسة وتحرس العمليات بصريًا وبرمجيًا. |
| جودة بيانات HR | جيد مبدئيًا | نموذج الموظف يمنع النقص الأساسي؛ يبقى نقل قواعد التحقق الحساسة إلى الخادم. |
| التدقيق | جيد للهوية، متوسط لـHR | هوية المستخدمين تسجل أحداثًا خادمية؛ HR يعرض سجلات عميلة قابلة للتزييف. |
| التجربة المرئية | جيدة | الهوية واضحة والمصفوفة احترافية، بينما حالات HR الفارغة ورسالة السنة الدراسية تحتاج تحسينًا. |

## خطة الإكمال المقترحة

1. تنفيذ `hr_employee_id` مع ترحيل، تحقق خادمي، واختيار الموظف من نموذج إنشاء/تعديل الحساب.
2. فصل صلاحية اعتماد HR خادميًا عن `Hr.Write` بعد تحديث قوالب الأدوار المنشورة.
3. نقل تحقق الرقم الوطني والتواريخ وIBAN إلى `validateHrSnapshotData`.
4. استبدال `auditLogs` العميلية بسجل خادمي قابل للاستعلام.
5. معالجة مصدر السنة الدراسية الموثوقة في البوابة المنشورة، ثم إعادة اختبار HR والهوية بعد نشر migration/build.

## نتيجة التدقيق

الوضع الحالي **متين للتطوير والاختبار، لكنه غير جاهز بعد لاعتماد تكامل HR/Identity نهائيًا**. أزيل خطر مصدر دليل الوظائف، وحُرست واجهة HR بالصلاحيات، وأضيف rollback للحفظ؛ ويبقى الربط المؤسسي المباشر للحساب بالموظف، والفصل الخادمي النهائي للاعتماد، وتوحيد التدقيق والتحقق الخادمي.

## ملحق اختبار القبول المرئي الموسع — 2026-09-13

أجري اختبار عملي على النسخة المنشورة من خلال المتصفح بعد تسجيل الدخول إلى مدرسة «مدارس الأسرة الحديثة»، مع استخدام بيانات اختبار اصطناعية فقط. لم تُنفذ أي دفعات مالية أو عمليات حذف.

### شؤون الطلاب

- عدد الطلاب بقي 0؛ لم يتم إنشاء الطلاب العشرة لأن الحفظ رفض السياق الأكاديمي برسالة: «السنة الدراسية الموثوقة غير صالحة أو غير محددة».
- بعد استكمال الاسم والرقم الوطني، ظهر إلزام تاريخ الميلاد وبيانات ولي الأمر من الخادم. أضيفت العلامات المرئية (*) إلى الحقول الإلزامية في المصدر المحلي.
- تبويبات أولياء الأمور والمستندات والتقارير وإعدادات التسجيل فتحت بصريًا، مع حالات فراغ مفهومة، دون تنفيذ رفع مستند أو تعديل إعدادات.

### رسوم الطلاب

- ظهرت حماية الحركات المالية بسبب عدم توفر مصدر مالي موثق.
- إنشاء بند رسوم وسند قبض والحفظ والاعتماد والترحيل والعكس والحذف كانت معطلة بصريًا أو مرفوضة بالحماية.
- لم يتم إنشاء رسوم مالية فعلية ولم يتم تسجيل أي قبض، وهو السلوك الصحيح أثناء غياب المصدر المالي الموثوق.

### شؤون العاملين

- أُنشئ قسم اختبار: `قسم اختبار الموارد البشرية`، بمعرف ظاهر `DEP-463`.
- أُنشئت وظيفة اختبار: `وظيفة اختبار إداري`، بمعرف ظاهر `JOB-928`، ودرجة «أ» وراتب 3500.
- أُنشئ موظف اختبار: `موظف اختبار إداري 01`، بمعرف ظاهر `EMP-2961`، ثم جرى إيقافه بنجاح؛ انخفض عدد الموظفين النشطين من 1 إلى 0.
- البريد المهني ظهر إلزاميًا في النسخة المنشورة، ورفض المتصفح الحفظ عند تركه فارغًا. هذا يخالف قاعدة «البريد اختياري»؛ المصدر المحلي يحتوي الإصلاح، لكن النسخة المنشورة لم تُعد بناؤها بعد.

### المستخدمون والصلاحيات

- بعد اكتمال التحميل ظهرت 6 حسابات و4 أدوار و133 صلاحية عبر 25 وحدة.
- تبديل صلاحية `Hr.Insert` ظهر بصريًا داخل المصفوفة، لكن نافذة النسخة المنشورة لا تحتوي زر حفظ للصلاحيات. عند الإغلاق وإعادة الفتح عادت الصلاحية غير محفوظة؛ لذلك لا يجوز اعتبار المنح ناجحًا.
- عند تأكيد إيقاف حساب `مستخدم اختبار الموارد البشرية` رفض الخادم العملية برسالة: «المستخدم غير موجود داخل مدرسة الجلسة الحالية». بقي الحساب نشطًا ولم يتم إيقافه.
- إنشاء حساب اختبار جديد لم يكتمل ولم يظهر في العدد أو القائمة، لذلك لم يُسجل كنجاح.

### العوائق المؤكدة في النسخة المنشورة

1. **P0:** عدم تطابق/فقدان السنة الدراسية الموثوقة بين الجلسة والواجهات، مما يمنع تسجيل الطلاب ويقفل الرسوم.
2. **P0:** تعارض نطاق هوية الحساب عند تنفيذ الإيقاف؛ الحساب يظهر في القائمة لكن مسار التعديل يرفضه كأنه خارج المدرسة الحالية.
3. **P1:** مصفوفة الصلاحيات تسمح بتغيير محلي بصري دون مسار حفظ ظاهر، ثم تفقد التغيير عند إعادة الفتح.
4. **P1:** النسخة المنشورة لا تزال تفرض البريد المهني رغم أنه يجب أن يكون اختياريًا.
5. **P1:** دليل الوظائف في نموذج المستخدمين المنشور لم يعكس وظيفة HR المضافة، وهو دليل إضافي على أن الإصلاح المحلي لم يُنشر بعد.

### حالة الإصلاح المحلي والنشر

- فحص TypeScript/lint المحلي ناجح، وفحص البناء المحلي ناجح.
- الإصلاحات المحلية تشمل توحيد دليل HR، حراسة صلاحيات HR، rollback للحفظ، وجعل بريد الموظف والمستخدم اختياريًا، وتحسين العلامات المرئية للحقول الإلزامية في الطلاب.
- يلزم نشر نسخة البناء الحالية ثم إعادة الاختبار المرئي، خصوصًا للسنة الدراسية، حفظ `Hr.Insert`، إيقاف المستخدم، واختيار وظيفة HR من نموذج الحساب.

### بيانات الاختبار المتبقية

بيانات القسم والوظيفة والموظف الاصطناعية ما زالت موجودة على المدرسة المختبرة، مع بقاء الموظف في حالة «موقوف». لم تُحذف حتى لا تُفقد أدلة الاختبار؛ حذفها يحتاج طلبًا منفصلًا وتأكيدًا صريحًا.

## إصلاحات منفذة بعد الاختبار

1. أزيلت القراءة المتقدمة من قناة Supabase البديلة في دليل مستخدمي المدرسة؛ أصبحت القائمة تقرأ من نفس PostgreSQL الكانوني الذي ينفذ الإيقاف والتعديل والصلاحيات، لمنع ظهور حساب لا يمكن تعديلُه في نفس النطاق.
2. أصبحت مطابقة السنة الدراسية تقبل بأمان تسمية العرض القديمة مثل `2026-2027` عند وجود سجل حالي موثق داخل نفس المدرسة والفرع، مع استمرار رفض المعرفات الأجنبية أو العشوائية.
3. أصبح مسار `/api/academic/context` مقيدًا بالمستأجر والمدرسة والفرع، ويختار السنة الفعالة الحالية حتى لا تتوقف شؤون الطلاب والرسوم بسبب اختلاف قيمة العرض عن معرف السجل.
4. أضيفت مؤشرات الإيرادات والمصروفات الحية من دفتر الأستاذ الكانوني إلى Dashboard، مع فصل الرقمين بدل عرض نفس الرقم تحت عنوانين متناقضين.

تم فحص TypeScript والبناء المحلي بنجاح بعد هذه الإصلاحات. ما زال يلزم نشر البناء إلى Render، ثم إعادة الاختبار المرئي للتأكد من ظهور الدخل الجديد، إنشاء طالب، حفظ صلاحية، وإيقاف مستخدم على النسخة المنشورة.
