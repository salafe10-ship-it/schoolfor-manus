# تقرير المراجعة الشاملة لمنظومة SchoolForManus

**تاريخ المراجعة:** 3 سبتمبر 2026  
**نوع المراجعة:** مراجعة عامة شاملة للقراءة فقط — دون إصلاح أو تعديل للكود أو البيانات  
**النطاق:** الإدارة المركزية، بوابات المدارس، الوحدات والشاشات والأزرار، الخادم وواجهات API، قاعدة البيانات، العزل متعدد المستأجرين، الأمن، الأداء، الاختبارات، الاعتماديات، التوثيق وتجربة الاستخدام.

---

## 1. الخلاصة التنفيذية

### القرار العام

**المنظومة لا تجتاز حاليًا بوابة الجاهزية للإطلاق الإنتاجي الكامل كمنتج ERP متكامل.**

هذا الحكم لا يعني أن كل الأجزاء غير صالحة. توجد نواة قوية فعلًا في قاعدة البيانات، المصادقة، الإدارة المركزية، شؤون الطلاب، القبول، وبعض المسارات المالية والامتحانات والموارد البشرية والمخزون. لكن توجد فجوة كبيرة بين هذه النواة وبين عدد من الشاشات التي ما زالت محاكاة أو حالة متصفح أو تخزينًا محليًا، وبعضها يعرض للمستخدم رسائل نجاح توحي بأن البيانات حُفظت أو رُحلت أو أُرسلت، بينما لا يحدث ذلك في مصدر مركزي موثوق.

أخطر نتيجة في المراجعة ليست نظرية: الفحص الحي لاتصال `DATABASE_URL` أظهر أن الحساب الحالي اسمه `postgres`، وبالرغم من أن `SUPERUSER=false` فإنه يحمل `BYPASSRLS=true`. قاعدة البيانات نفسها تفعّل وتفرض RLS على كل الجداول ذات نطاق المستأجر التي فُحصت، لكن الحساب المتصل يستطيع تجاوز هذه السياسات. يوجد حساب آمن باسم `edupro_app` (`rolsuper=false`, `rolbypassrls=false`) إلا أن الاتصال الحالي لا يستخدمه. كما أن متغير `EDUPRO_ENVIRONMENT` غير مضبوط، ولذلك لم تُفعّل بوابة الرفض الصارمة الموجودة في الخادم.

### التصنيف المختصر

| المجال | الحكم | الملاحظة الأساسية |
|---|---|---|
| مخطط قاعدة البيانات والمهاجرات | قوي | 73 جدولًا عامًا حيًا، 67 جدولًا بنطاق tenant، وRLS مفعّل ومفروض عليها جميعًا |
| اتصال قاعدة البيانات الحي | حرج | `DATABASE_URL` يستخدم حسابًا يملك `BYPASSRLS` |
| المصادقة والصلاحيات على الخادم | جيد مع مخاطر | هوية وصلاحيات مشتقة من جلسة موثقة، لكن تخزين الرموز في المتصفح وسياسات الرؤوس تحتاج معالجة |
| الإدارة المركزية | جيد جزئيًا | الدليل المركزي والمدارس والفروع والمستخدمون وRBAC حقيقية؛ النسخ والمراقبة والنشر والدعم المؤقت غير موصولة |
| شؤون الطلاب والقبول | الأقوى وظيفيًا | مسارات خادم، صلاحيات، عزل، تدقيق، مستندات ودورات حياة حقيقية |
| الامتحانات والموارد البشرية والمخزون | متصل جزئيًا | حفظ مركزي بإصدارات، لكنه يعتمد على لقطات JSONB كبيرة لكل مدرسة |
| الحسابات ورسوم الطلاب | مختلط | توجد جداول ومسارات دفتر أستاذ حقيقية، مع استمرار أوضاع snapshot وUAT ومسارات محلية قديمة |
| الأكاديمي والحضور وأولياء الأمور والزي | غير جاهز | عمليات محلية/محاكاة مع رسائل نجاح غير حقيقية في عدة أزرار |
| المكتبة والنقل والأصول المستقلة والإعدادات | غير مكتمل | ربط مباشر غير سليم أو تخزين محلي محجوب في الإنتاج أو وظائف غير موصولة |
| الذكاء الاصطناعي | يحتاج حوكمة قبل الاستخدام | إرسال سياق طلاب/امتحانات/تدقيق إلى مزود خارجي، وتنبؤ مالي مبني على مدخل العميل |
| الاختبارات | واسعة لكن غير كافية للإطلاق | 323 ملف اختبار و845 اختبارًا ناجحًا، لكن غالبية كبيرة منها اختبارات عقد نصية وليست E2E |
| تجربة الاستخدام والتنقل | غير مكتملة | شريط التنقل الجانبي أزيل، وعدة وحدات أصبحت غير قابلة للوصول من الواجهة الرئيسية |

---

## 2. ما تم فحصه وكيفية التحقق

تمت مراجعة ما يلي دون تنفيذ أي إصلاح:

- 2,068 ملفًا خارج مجلدات الاعتماديات والبناء، منها 917 ملف TypeScript/TSX ونحو 233,195 سطرًا.
- 38 ملف migration داخل `supabase/migrations`، إضافة إلى مخططات وأدوات تحقق أخرى.
- ملف الخادم الرئيسي `server.ts`، ونطاقات المصادقة وRBAC والعزل والتدقيق والمعاملات.
- بوابة الإدارة المركزية وجميع تبويباتها المرئية.
- بوابة المدرسة ووحداتها الرئيسية والفرعية.
- فحص ساكن لـ 1,205 عنصر `<button>` داخل المكونات والوحدات التشغيلية.
- تشغيل الحزمة الإنتاجية الموجودة محليًا، وفتح شاشة الدخول فعليًا في المتصفح، وفحص سجل المتصفح.
- فحص حي لنقاط الصحة والجاهزية والحماية من الوصول غير المصادق.
- استعلامات قراءة فقط على قاعدة البيانات للتحقق من الدور، الجداول، RLS، الفهارس والدوال المطلوبة.
- تشغيل TypeScript وفحوص Vitest وتدقيق اعتماديات الإنتاج.

### نتائج التنفيذ والتحقق

- `npm run lint`: **ناجح**، ولا توجد أخطاء TypeScript وفق الإعداد الحالي.
- `vitest run`: **323 ملف اختبار ناجح، 845 اختبارًا ناجحًا**.
- ظهر تحذير React واحد في `studentAffairsImport.test.tsx` عن تحديث حالة غير ملفوف بـ `act(...)`.
- `pnpm list --depth 0`: ناجح، وبيئة الاعتماديات قابلة للقراءة عبر pnpm.
- `pnpm audit --prod --audit-level=moderate`: كشف **ثغرتين بدرجة Moderate** في `qs` عبر Express/body-parser:
  - `GHSA-x5fp-wj9c-mxmx`
  - `GHSA-4mjr-xmp4-gh2g`
- الحزمة الإنتاجية الحالية بدأت بنجاح على المنفذ 3000.
- `GET /api/health`: 200 وحالة `available`.
- `GET /api/ready`: 200 وحالة `ready`.
- `GET /api/students` دون رمز: 401.
- `POST /api/ai/forecast` دون رمز: 401.
- صفحة الدخول ظهرت دون أخطاء Console ظاهرة.

### حدود المراجعة

- لم تُستخدم بيانات دخول تجريبية أو حقيقية، ولذلك لم تُنفذ جولة متصفح كاملة داخل الشاشات المحمية.
- لم تُضغط أزرار قد تكتب أو تحذف أو ترسل بيانات.
- لم يُشغّل build جديد حتى لا يتغير مجلد `dist`؛ تم تشغيل الحزمة الموجودة، بينما فُحص المصدر الحالي بالـ TypeScript والاختبارات.
- تعذر تشغيل الأدوات المبنية على `tsx` في بيئة المراجعة بسبب خطأ مضيف Windows/Node: `uv_os_get_passwd returned ENOMEM`. تم تجاوز ذلك فقط في التحقق القرائي لقاعدة البيانات باستخدام Node و`pg` مباشرة، دون كتابة.
- لم تُنفذ تغطية اختبارية `coverage` ولا اختبار تحميل حقيقي ولا فحص اختراق خارجي.

---

## 3. النتائج الحرجة والعالية

### C-01 — اتصال البيانات الحالي يتجاوز RLS

**الدرجة: حرجة**

النتيجة الحية:

- قاعدة البيانات: `postgres`
- المستخدم الحالي: `postgres`
- `rolsuper=false`
- `rolbypassrls=true`
- متغير `EDUPRO_ENVIRONMENT` غير مضبوط.
- متغير `PLATFORM_ADMIN_DATABASE_URL` غير مضبوط؛ لذلك قد يعود مسار الإدارة المركزية إلى نفس `DATABASE_URL`.

في المقابل:

- يوجد دور `edupro_app` بصلاحيات `rolsuper=false`, `rolbypassrls=false`, `rolcanlogin=true`.
- 67 جدولًا ذا عمود `tenant_id` تم فحصها حيًا، ولم يوجد جدول بينها دون RLS أو دون FORCE RLS.

**الأثر:** حماية RLS موجودة في المخطط لكنها لا تحمي من الحساب الحالي. أي خطأ في شرط نطاق داخل الخادم يمكن أن يتحول إلى قراءة أو كتابة عبر المستأجرين. بوابة الخادم ترفض هذا الدور فقط إذا كان `EDUPRO_ENVIRONMENT=staging|production`؛ غياب المتغير يجعل `/api/ready` يعيد Ready رغم الدور غير الآمن.

**الأدلة:** `server.ts:1506-1555`، `server/infrastructure/PostgresTransactionDriver.ts`، والمخرجات الحية للاستعلامات القرائية.

### H-01 — سلسلة مخاطر XSS والجلسة أقوى من كل حاجز منفرد

**الدرجة: عالية**

- Helmet يعطّل `Content-Security-Policy` و`frameguard` صراحة في `server.ts:1462-1466`.
- خيار «تذكرني» مفعّل افتراضيًا في شاشة الدخول.
- رمزا access وrefresh يُحفظان في `localStorage` عند التذكر، أو في `sessionStorage` عند عدمه، داخل `src/middleware/trustedSessionManager.ts`.
- توجد 34 عملية كتابة مباشرة إلى نافذة طباعة عبر `document.write`، وبعضها يدمج حقولًا أدخلها المستخدم دون escaping. المثال الواضح هو أسماء ورموز المواد والمعلمين والمدرسة في `AcademicAffairsPortal.tsx:326-384`.

**الأثر:** أي XSS ناجح يستطيع قراءة access/refresh tokens، ومع غياب CSP تقل طبقة الدفاع. تعطيل frameguard يسمح أيضًا بإدراج التطبيق داخل iframe، ما يزيد مخاطر clickjacking ما لم توجد حماية مكافئة على البنية الأمامية.

### H-02 — رسائل نجاح غير حقيقية في الشؤون الأكاديمية

**الدرجة: عالية**

`AcademicAffairsPortal` لا يستدعي API أو Repository. المواد والجداول والإعدادات تُدار بحالة React فقط، مع أزرار تعرض:

- تم إضافة/تعديل/حذف المادة بنجاح.
- تم إضافة الحصة بنجاح.
- تم اعتماد العام الأكاديمي لكل الشاشات.
- تم حفظ السياسات الأكاديمية.

بعد تحديث الصفحة تضيع هذه التغييرات. كما تُولّد أكواد المواد بـ `Math.random`.

**الأثر:** تضليل تشغيلي وفقدان بيانات وتعارض مباشر مع وصف النظام كمنظومة مركزية.

### H-03 — الحضور وأولياء الأمور ليسا وحدتين مركزيتين فعليتين

**الدرجة: عالية**

في `App.tsx:1979-2078`:

- زر اعتماد التحضير لا يحفظ شيئًا؛ يعرض إشعار نجاح فقط.
- أزرار حاضر/غائب تغيّر حالة React المحلية.
- زر مراسلة ولي الأمر يعرض نجاح إرسال رسالة دولية دون موصل SMS أو طلب خادم.

**الأثر:** سجلات حضور ورسائل حساسة قد يعتقد الموظف أنها اعتمدت أو أُرسلت بينما لم يحدث ذلك.

### H-04 — وحدة الزي المدرسي محرك محاكاة يعلن COMMIT سحابيًا

**الدرجة: عالية**

`SchoolUniformManagement.tsx` يستخدم `SQLTransactionEngine`، وهو موصوف داخل الكود بأنه محرك محاكاة، ويحتفظ بسجل المعاملات في الذاكرة ويضيف تأخيرًا اصطناعيًا. كتل التنفيذ تغيّر state محلية، بينما الرسائل تعلن نجاحًا في قاعدة البيانات أو COMMIT وتزامنًا سحابيًا.

كما توجد هوية وعناوين IP ثابتة داخل العمليات:

- المستخدم `salafe10@gmail.com`
- عناوين مثل `192.168.10.15` وغيرها.
- كتل authorization تعيد `{ authorized: true }` محليًا.

**الأثر:** عدم موثوقية سجل التدقيق، نسبة العمليات إلى شخص غير المستخدم الحالي، وإيهام بأن تعليمات SQL المعروضة قد نُفذت فعليًا.

### H-05 — المكتبة والنقل مربوطان بطبقة Supabase مباشرة غير متوافقة مع الجلسة والنطاق

**الدرجة: عالية**

`LibraryRepository` و`TransportationRepository` و`StudentTransportationRepository` تُستدعى من المتصفح مباشرة عبر Supabase client عام:

- العميل العام مضبوط بـ `persistSession:false` ولا يحمل bearer token الخاص بجلسة التطبيق.
- سياسات الجداول اللوجستية تعتمد `app.tenant_id` و`app.school_id` داخل جلسة PostgreSQL.
- عمليات `student_transportation` في الواجهة لا ترسل حقول `tenant_id`, `school_id`, `branch_id` المطلوبة في المخطط الحي، وتستخدم أسماء camelCase مقابل أعمدة snake_case.
- بعض استعلامات النقل لا تضيف شرط school إلى الاستعلام، اعتمادًا على RLS الذي لا تملك جلسة المتصفح سياقه أصلًا.
- سجل النقل يُرشّح بعد القراءة إلى الطلاب الموجودين في state المشتركة؛ هذه القائمة تبدأ فارغة ولا تُحمّل خارج بوابة شؤون الطلاب، ما قد يخفي اشتراكات حقيقية.

المكتبة نفسها:

- الفهرس فقط يحاول القراءة/الكتابة.
- قائمة الإعارات تبدأ فارغة ولا تُحمّل.
- إغلاق الإعارة وترحيل الغرامة والاستيراد معلنة كغير متاحة.
- زر Excel ينتج CSV، وزر PDF يفتح الطباعة فقط.

**الأثر:** هذه الوحدات ستفشل مغلقًا غالبًا في build الإنتاج، أو تظهر فارغة، رغم وجود جداول لوجستية حية.

### H-06 — بوابة الأصول الثابتة المستقلة غير مرتبطة بمصدر محاسبي مركزي

**الدرجة: عالية**

`FixedAssetsPortal` يعتمد `FixedAssetsRepository` المبني على `localStorage`. في وضع الإنتاج يمنع `FallbackStorage.assertCanonicalPersistence` القراءة والكتابة، فتظهر البوابة متوقفة. ومع ذلك تحتوي الواجهة على أزرار حفظ ورسملة ونقل وصيانة وإهلاك وبيع واستبعاد ورسائل نجاح.

توجد شاشة أصول أخرى داخل الأستاذ العام تستخدم snapshot المالي؛ النتيجة هي وجود مسارين مختلفين للأصول بمصادر حقيقة مختلفة.

### H-07 — الإعدادات العامة ليست مصدر حقيقة رغم صياغة الواجهة

**الدرجة: عالية**

`SystemSettingsPortal` يحتوي 11 تبويبًا، لكن:

- الحفظ الأساسي يعرض تحذيرًا صريحًا أن خدمة الحفظ المركزية غير متاحة.
- إضافة البيانات المرجعية وحذفها تغيّر state محلية فقط، مع رسالة نجاح.
- تصدير/استعادة إعدادات وفحص «Zero Error» تعرض نجاحًا دون تنفيذ فعلي موثق.
- إعداد العملة في `App.tsx` يُحفظ في `localStorage` بينما النص يقول إنه حُفظ في جدول الإعدادات وطُبق على النظام الحي.
- نافذة إعداد Supabase تغيّر state محلية وتعلن نجاح الاتصال دون فحص أو حفظ خادمي.

**الأثر:** الإعدادات الحرجة قد تبدو مطبقة وهي محصورة في جهاز واحد أو غير محفوظة أصلًا.

### H-08 — مركز صحة النظام يجمع بين مسارات غير متاحة وتقارير تحميل اصطناعية

**الدرجة: عالية**

- الواجهة تقرأ الرمز من `localStorage.getItem('token')`، بينما مدير الجلسة يستخدم `edupro_token`؛ لذلك طلبات المراقبة ستخرج غالبًا دون الرمز الصحيح.
- كل endpoints الخاصة بقياسات DB والتنبيهات والمحاكاة والتحسين والنسخ موجهة حاليًا إلى `databaseObservabilityUnavailable` وتعيد عدم توفر.
- اختبار التحمل في الواجهة لا يولد حملًا حقيقيًا؛ يستخدم مراحل وأرقامًا ثابتة مع variation عشوائي، ثم يصدر تقييم A/A+ وادعاء تأهيل آلاف المدارس.
- شرط تقييم 3000 مدرسة غير قابل للوصول لأن شرط `>=2000` يسبقه.
- فحص الصحة العام نفسه أصبح يفشل مغلقًا برسالة صحيحة، لكن بقي وراء `return` كود قديم يولد نتيجة 97% اصطناعية.

**الأثر:** الوحدة لا تصلح كدليل تشغيل أو شهادة أداء. الأرقام المعروضة في المحاكاة ليست قياسات من الخادم.

### H-09 — مخاطر خصوصية وحوكمة في الذكاء الاصطناعي

**الدرجة: عالية**

`POST /api/ai/chat` يجلب طلاب المدرسة والامتحانات وآخر سجلات التدقيق، ثم يضع البيانات المطابقة في system instruction ويرسلها إلى Gemini. توجد محاولة لتقليل السياق بالكلمات المفتاحية، لكنها ليست سياسة data minimization موثوقة:

- كلمات وأسماء ثابتة خاصة ببيانات seed مستخدمة في المطابقة.
- عند عدم وجود مطابقة قد يرسل أول خمسة طلاب.
- بيانات الامتحانات يمكن إرسالها كاملة.
- لا توجد قائمة حقول مسموح بها أو إزالة صريحة لأرقام الهوية/الاتصال/المعلومات الحساسة قبل الإرسال.
- لا تظهر آلية موافقة، تصنيف بيانات، سياسة احتفاظ، أو سجل نقل خارجي مخصص.

أما `POST /api/ai/forecast` فيقبل قائمة الطلاب والأرصدة من جسم طلب العميل، ثم يطبق نسبًا ثابتة 85% و40% ويصفها بأنها `AI Heuristics Engine v2.1`. المدخل قابل للتلاعب ولا يأتي من قراءة خادم موثوقة.

**الأثر:** خطر إفشاء بيانات شخصية إلى مزود خارجي، ومخرجات مالية غير قابلة للاعتماد كمؤشر تنبؤي.

### H-10 — شاشة SQL تعرض مخططًا قديمًا وغير مكافئ للمهاجرات الحية

**الدرجة: عالية**

شاشة `db_schema` تطلب من مدير المنصة نسخ SQL ولصقه في Supabase «لبناء المنظومة بضغطة واحدة». النص يأتي من `src/database/seed/mockData.ts` ويحتوي عددًا محدودًا من الجداول القديمة مثل schools/branches/students/teachers/invoices/audit_logs/system_settings.

المخطط الحي الحالي يحتوي 73 جدولًا و38 migration وطبقات tenant وRBAC ووثائق ومالية وامتحانات وHR ومخزون ولوجستيات. SQL المعروض لا يمثل هذا المخطط، ولا يستخدم آلية migration الموثوقة، وقد يصطدم بجداول قائمة لأنه يستخدم `CREATE TABLE` مباشرة.

### H-11 — الواجهة الرئيسية لا تتيح الوصول المتسق إلى كل الوحدات

**الدرجة: عالية وظيفيًا**

- `Sidebar` مستورد لكنه غير معروض، ويوجد تعليق: `Sidebar removed completely as requested`.
- لوحة المدرسة تعرض 12 اختصارًا فقط.
- وحدات القبول، أولياء الأمور، المكتبة، الفروع، الأصول المستقلة، الإعدادات، التدقيق والمراجعة العامة غير موجودة ضمن اختصارات اللوحة.
- بعض الوحدات يمكن الوصول إليها عبر بطاقة مؤشر أو من داخل وحدة أخرى، لكن لا توجد خريطة تنقل كاملة.
- التطبيق لا يستخدم Router فعليًا؛ التنقل state محلية، ولذلك لا توجد روابط عميقة موثوقة أو تاريخ تنقل لكل شاشة.
- بحث الطلاب في Topbar يستقبل `students=[]` افتراضيًا لأن `App` لا يمرر الخاصية إليه؛ يظهر النص «مربوط بالـ DB» لكنه لن يجد طلابًا.
- شاشة الإنتاج الخاصة بالمدرسة تخفي AI والتدقيق والصلاحيات والإعدادات والمراجعة وكل صفحات الإدارة المركزية حتى لو وردت الصلاحية، وفق `CustomerProductionPortalPolicy`.

**الأثر:** وجود الوحدة في الكود أو الصلاحيات لا يعني أن المستخدم يستطيع الوصول إليها في المسار التشغيلي الحقيقي.

---

## 4. مراجعة الإدارة المركزية

### التبويبات الموجودة

1. لوحة العمليات.
2. إدارة المستأجرين.
3. المدارس.
4. الفروع.
5. المستخدمون.
6. إدارة الاشتراكات.
7. إدارة الوحدات والميزات.
8. الروابط/النطاقات السحابية.
9. Developer & Platform Center.
10. لوحة التشغيل العامة.
11. التقارير والإحصائيات والموارد.
12. النسخ الاحتياطية.
13. مركز التنبيهات.
14. الدعم الفني المؤقت/impersonation.
15. RBAC المعروض بعنوان «إدارة التراخيص».
16. التحديثات/الإعدادات العامة.
17. سجل النشاط.
18. سلامة النظام.

### ما هو متصل فعليًا

- دليل المستأجرين: قراءة وإنشاء وتعديل وحالات lifecycle مركزية.
- المدارس: قراءة وإنشاء وتعديل ملفات المدارس.
- الفروع: قراءة وإنشاء وتعديل.
- المستخدمون: قراءة، provisioning، وتعديل الحالة/الخصائص.
- RBAC المركزي: قراءة الأدوار والصلاحيات وتعديل role permissions.
- الاشتراكات والوحدات والنطاقات: الحفظ يمر عبر تحديثات tenant/school مركزية.
- التنبيهات المركزية: قراءة وإنشاء عبر API.
- سجل النشاط المركزي: قراءة من endpoint مخصص.
- الصحة المركزية الأساسية: endpoint حقيقي، مع عرض حالات عدم التوفر بدل اختلاق بيانات.

### ما هو جزئي أو غير متاح

- الحوادث والقياسات التفصيلية لا يوجد لها مصدر Incident/observability موثق.
- صيانة قواعد البيانات والتخزين المؤقت غير موصلة.
- النسخ الاحتياطي، الجدولة، التنزيل، فحص السلامة، الاستعادة واختبار التعافي لا تملك مزودًا مركزيًا؛ الواجهة تفشل مغلقًا برسائل صحيحة.
- impersonation لا ينشئ جلسة مركزية قصيرة العمر.
- النشر والrollback لا يملكان خدمة نشر مركزية، وسجل الإصدارات فارغ.
- Developer Center لا يمثل منصة تشغيل مكتملة.

### نقاط جيدة في الإدارة المركزية

- لا تعتمد الشاشات المركزية الأساسية على `localStorage` كمصدر للمدارس والمستأجرين.
- الرسائل في الشاشات غير الموصولة أصبحت في أغلبها صادقة: «لم يتم التنفيذ» بدل نجاح وهمي.
- الخادم يطلب `Platform.Admin` لكل مسارات التحكم المركزي.
- الفحص الحي أثبت وجود الجداول المطلوبة: tenants, subscriptions, schools, branches, users, platform_users.
- الفحص الحي أثبت وجود دوال نطاق الهوية الأربع وفهرسي uniqueness للنطاقات، وعدم وجود مجموعات نطاقات مكررة حاليًا.

### ملاحظات تجربة الاستخدام

- يوجد تداخل دلالي بين «لوحة العمليات» و«لوحة التشغيل العامة».
- تبويب RBAC مسمى «إدارة التراخيص»، ما يخلط الترخيص التجاري بصلاحيات الوصول.
- التنقل داخل الإدارة المركزية state محلية وليس URL، فلا توجد روابط مباشرة للتبويبات أو Back/Forward طبيعي.

---

## 5. مراجعة بوابة المدارس والوحدات

### 5.1 لوحة المدرسة

**المحتوى:** مؤشرات الحضور والمصروفات والإيرادات والفصول والموظفين والمعلمين والطلاب، واختصارات الطلاب والحسابات والأكاديمي والامتحانات وHR والرسوم وAI والمخزون والجداول والنقل والزي والصلاحيات.

**الجيد:** المؤشرات تستخدم `/api/dashboard/metrics` وتعرض `—/غير متاح` عند غياب المصدر بدل أرقام seed.

**المشكلات:**

- إشعارات Topbar تبدأ بثلاث رسائل seed، منها نجاح مزامنة Supabase وصدور تقارير مالية، ولا تُصفّر عند دخول customer production.
- البحث العالمي للطلاب غير موصول ببيانات الطلاب كما سبق.
- عدة وحدات لا يوجد لها اختصار بعد إزالة Sidebar.

### 5.2 شؤون الطلاب

**الحكم: الأقرب إلى الجاهزية التشغيلية.**

**الشاشات:** بيانات الطالب، أولياء الأمور، المستندات، التقارير، الإعدادات؛ ونموذج الطالب يشمل الأساسي، ولي الأمر، بيانات إضافية، المستندات، الملاحظات.

**القدرات المركزية:**

- قائمة وبحث وفلاتر وترقيم صفحات وترتيب.
- إضافة وتعديل وأرشفة وحذف منطقي واستعادة.
- نقل، ترقية، إعادة قيد، تخرج، فصل وأرشفة.
- استيراد جماعي وتصدير XLSX من الخادم.
- ربط ولي أمر وتدقيق تغييرات.
- مستندات، نسخ مستندات، تحقق، أرشفة، تنزيل وسجل وصول.
- timeline مركزي ودورة حياة enrollment.
- صلاحيات مستقلة للقراءة والكتابة والحذف والتصدير والمستندات.

**الملاحظات:**

- الرفع الفعلي لبعض عناصر الصورة/الاتصال معلن كغير متاح في الواجهة.
- الاستدعاءات اليدوية في بعض الوحدات المجاورة لا تستخدم دائمًا `authenticatedRequest` الموحد.
- يوجد helper قديم `updateStudentField` بمحاكاة autosave، ويجب ألا يُعد دليلًا على حفظ مركزي.

### 5.3 القبول والتسجيل

**الحكم: متصل مركزيًا.**

سير الحالات: `INQUIRY → VERIFIED → FEE_PAID → ENROLLED` أو `REJECTED`. توجد قراءة/إنشاء وتحديث حالة، بحث وفلاتر وترقيم صفحات وإعادة محاولة. الخادم يطبق صلاحيات Admission.Read/Write.

### 5.4 الشؤون الأكاديمية والجداول

**التبويبات:** الهيكل، المواد، الفصول، الجدول، التحليلات، الإعدادات.

**الحكم:** واجهة واسعة لكن CRUD محلي وغير محفوظ. الرسائل الحالية تبالغ في النجاح. تصدير CSV والطباعة يعملان من البيانات الموجودة في الذاكرة، لكن الطباعة تحتوي فجوة XSS.

### 5.5 الحضور والانصراف

**الحكم:** نموذج محلي بسيط، وليس وحدة حضور مركزية. لا توجد endpoint تشغيلية للحضور في `server.ts` رغم وجود طبقات domain/repository قديمة في المصدر.

### 5.6 أولياء الأمور

**الحكم:** شاشة مشتقة من قائمة الطلاب وليست بوابة ولي أمر. زر المراسلة لا يرسل رسالة. لا يوجد سجل اتصال أو مزود SMS.

### 5.7 الامتحانات والنتائج

**الحكم: متصل مركزيًا جزئيًا، واسع جدًا، ويحتاج E2E حقيقي.**

**التبويبات الـ17:**

1. مركز عمليات الكنترول.
2. إعدادات الامتحانات.
3. الفصول والمواد.
4. بنك الأسئلة والامتحان الإلكتروني.
5. اللجان والقاعات.
6. توزيع الطلاب.
7. أرقام الجلوس.
8. المراقبون.
9. الجدول.
10. إدخال الدرجات.
11. معالجة النتائج.
12. الجودة والحوكمة.
13. المراجعة والاعتماد.
14. التقارير.
15. الشهادات وكشوف الدرجات.
16. الإعدادات العامة.
17. دليل الكنترول.

**نقاط جيدة:** snapshot مركزي بإصدار وتعارض optimistic concurrency، مزامنة صفوف من السجل الأكاديمي، تدقيق مركزي، أرشيف نتائج، RBAC، تحقق من الإغلاق والحسابات.

**مخاطر:** الملف الرئيسي يقارب 573KB، والحالة الكاملة تحفظ في JSONB واحدة لكل مدرسة. هذا يرفع تكلفة التزامن والاختبار والترحيل، ويجعل تعارض تعديلين مستقلين تعارضًا على snapshot كامل. المقارنات التاريخية محدودة إلى أن تُعرض ملخصات الأرشيف من API.

### 5.8 الموارد البشرية والرواتب

**المجالات:** الموظفون، الهيكل، الأقسام، الوظائف، العقود، الحضور، الإجازات، السلف، المكافآت، الجزاءات، الرواتب، التقارير، الأداء، المستندات، الإعدادات والاعتماد.

**الحكم:** حفظ مركزي بإصدار في `hr_database`، مع مسارات خادم لعقود التوقيع، خرائط الحسابات، صرف السلف، اعتماد وصرف الرواتب، وتدقيق التقارير.

**المخاطر:** snapshot JSONB كامل، auto-save متعدد الحالات، واستدعاءات fetch يدوية لا تستفيد من refresh/retry الموحد. الشهادات والمستندات ليست كلها موصولة كمستودع ملفات فعلي.

### 5.9 الحسابات العامة ودفتر الأستاذ

**التبويبات:** لوحة التحكم، شجرة الحسابات، مراكز التكلفة، القيود، القبض، الصرف، الحوالات، الخزينة والبنوك، ذمم الطلاب، الموردون، الأصول، التقارير، السياسات، الإقفال، المستخدمون، أدوات الحسبة.

**الحكم:** مختلط، مع تمييز جيد داخل الواجهة بين الحالات التالية:

- `snapshot_read_only`
- `snapshot_write` لاستخدام UAT
- `erp_integrated`
- `ledger_ready`

توجد جداول فعلية للقيود والسطور ودفتر الأستاذ والفترات وخرائط الحسابات، وحماية append-only وعكس بدل حذف. وتوجد مسارات حقيقية لعكس قيد وترحيل سند قبض.

**المخاطر:**

- استمرار كم كبير من النماذج المحلية وlegacy localStorage داخل `App` ومتجر المحاسبة.
- بعض الشاشات متاحة للحفظ كـ snapshot UAT وليست ترحيل GL معتمد.
- ازدواج الأصول بين بوابة مستقلة محلية وتبويب داخل الأستاذ العام.
- الملفات المحاسبية شديدة الضخامة، ما يرفع احتمال regression.

### 5.10 رسوم وحسابات الطلاب

**التبويبات:** التحليلات، إعدادات الرسوم، التوزيع الجماعي، الإدارة، سندات القبض، التقارير، ودورة الرقابة والترحيل.

**الحكم:** توجد جداول مركزية للفواتير والإيصالات والقيود والسطور والإعدادات والتدقيق، مع snapshot توافقية. العمليات المهمة تحاول انتظار الكتابة المركزية قبل نشر النجاح.

**المخاطر:** استمرار تبويب يحمل مفهوم demo، واستعمال snapshot كبيرة، ووجود مسار قديم في `App` لا يحفظ إلا محليًا (وهو يفشل مغلقًا عندما يكون الوضع canonical).

### 5.11 المخزون والمشتريات

**المخزون:** لوحة، الأصناف، التصنيفات، الموردون، المستودعات، الحركات، الجرد، المشتريات، التقارير، التدقيق، الإعدادات.

**المشتريات:** لوحة، طلبات شراء، RFQ، أوامر شراء، استلام GRN، فواتير ومدفوعات، موردون، تقارير، إعدادات.

**الحكم:** متصل بخادم عبر `inventory_database` بإصدار وتحقق من snapshot وتدقيق تصدير التقارير. أفضل من وحدات المحاكاة، لكنه ما يزال يحفظ كل النطاق في JSONB واحدة.

### 5.12 المكتبة

**التبويبات:** الفهرس، الإعارات، الغرامات.

**الحكم:** غير مكتملة. الفهرس يحاول Supabase مباشرًا، لكن دورة الإعارة غير محملة، والإرجاع والغرامات والاستيراد غير متاحة. تنسيق التصدير لا يطابق دائمًا اسم الزر.

### 5.13 النقل المدرسي

**التبويبات:** المؤشرات، الأسطول والمسارات، اشتراكات الطلاب، السلامة، التقارير.

**الحكم:** الجداول الحية موجودة ومحمية، لكن UI/Repository الحالية لا تمر عبر خادم ذي tenant context، وعقود الأعمدة لا تطابق المخطط بالكامل. الوحدة غير مثبتة تشغيليًا.

### 5.14 الزي المدرسي

**التبويبات:** لوحة، الأصناف، المشتريات، المبيعات، المرتجعات، المحاسبة، المخطط.

**الحكم:** محاكاة داخل المتصفح وليست وحدة canonical.

### 5.15 الأصول الثابتة

**التبويبات المستقلة:** لوحة، سجل، تقارير وإهلاك، تدقيق جودة.

**الحكم:** البوابة المستقلة محجوبة عمليًا في الإنتاج لعدم وجود مصدر مركزي. تبويب الأصول داخل المحاسبة له مسار مختلف وجاهزية مشروطة بوضع المالية.

### 5.16 المستخدمون والصلاحيات

**الجيد:** يوجد RBAC مركزي حقيقي للإدارة المركزية، وقاموس صلاحيات واسع للعرض/الإضافة/التعديل/الحذف/الاعتماد/الإلغاء/الترحيل/العكس/التصدير/الطباعة، ونطاقات مؤسسة/فرع/سنة/مستودع/خزينة/شخصي.

**المشكلة:** شاشة المدرسة نفسها تعتمد fixtures وlocalStorage في الوضع الداخلي، وتتحول إلى read-only فارغ في الوضع canonical، وهي مخفية أصلًا في customer production. توجد ثلاثة أزرار أيقونية مؤكدة بلا handler: المظهر، جرس يحمل رقمًا ثابتًا 12، والمساعدة. الصورة الرمزية تُجلب من Unsplash خارجيًا.

### 5.17 الإعدادات

**التبويبات:** لوحة، المؤسسة، المدرسة، المالية، الامتحانات، الرسوم، HR، النظام/API، master data، التدقيق، النسخ الاحتياطي.

**الحكم:** شاشة تصميمية أكثر من كونها مركز إعدادات. معظم الحفظ غير موصول، وبعض الأزرار تعرض نجاحًا دون تنفيذ.

### 5.18 السجلات والمراجعة والصحة

- سجل التدقيق: يوجد endpoint مركزي حقيقي، لكن توجد بالتوازي سجلات محلية وهويات/IP ثابتة في `App` ومحركات المحاكاة.
- المراجعة العامة: معلنة بوضوح «قيد التجهيز» ولا تحتوي إجراءات أو بيانات.
- صحة النظام: مزيج من unavailable صادق ومحاكاة أداء غير حقيقية.
- صفحة SQL: لا تمثل مخطط الإنتاج الحالي.

### 5.19 المساعد الذكي

- الوضع السحابي يستدعي API حقيقيًا.
- وضع المحاكاة لا يعرض بيانات مؤسسية، وهذه نقطة جيدة.
- الوحدة مخفية في customer production حاليًا.
- المخاطر الأساسية هي نقل البيانات الخارجية، تقليل البيانات، ونموذج forecast غير canonical.

---

## 6. مراجعة الأزرار والتفاعلات

تم إحصاء **1,205 زرًا أصليًا** في `src/components` و`src/modules`. هذا العدد الكبير لا يمكن اعتماده بمجرد وجود `onClick`؛ المراجعة فرّقت بين:

1. زر ينفذ طلب خادم موثوقًا وينتظر النتيجة.
2. زر يغير state محلية فقط.
3. زر يعرض رسالة نجاح فقط.
4. زر يعرض «غير متاح» بصورة صادقة.
5. زر بلا handler أو غير قابل للوصول بسبب التنقل.

### النتائج الرئيسية

- كثافة الأزرار الأعلى في الامتحانات، رسوم الطلاب، HR، شجرة الحسابات، شؤون الطلاب والخزينة.
- اختبارات wiring الحالية تغطي أجزاء محدودة، خصوصًا بعض ملفات الامتحانات، وليست اختبارًا لكل الأزرار.
- تأكد وجود أزرار أيقونية خاملة في شاشة صلاحيات المدرسة.
- زر دعم «تواصل الآن» في Sidebar بلا handler، لكن Sidebar نفسه غير معروض.
- أزرار المراسلة والحضور والأكاديمي والزي والإعدادات تتضمن حالات نجاح لا يقابلها تنفيذ مركزي.
- أزرار غير متاحة في شؤون الطلاب والمكتبة والإدارة المركزية تستخدم غالبًا رسائل صادقة، وهذه ممارسة أفضل.
- لا يوجد دليل E2E على الحالات: double click، انتهاء الجلسة أثناء الحفظ، 409 version conflict، ضعف الشبكة، فتح نافذة الطباعة المحجوبة، أو تشغيل الهاتف/لوحة المفاتيح.

---

## 7. قاعدة البيانات والعزل والتدقيق

### نقاط القوة المثبتة حيًا

- 73 جدولًا عامًا في قاعدة البيانات الحالية.
- 67 جدولًا بنطاق `tenant_id`.
- `RLS disabled = 0` لهذه الجداول.
- `RLS not forced = 0` لهذه الجداول.
- الجداول الأساسية موجودة: الطلاب، أولياء الأمور، القيد، المستندات، التدقيق، المالية، دفتر الأستاذ، الامتحانات، HR، المخزون، الحافلات، النقل، الزي، RBAC، تخزين مستندات الطلاب والتخرج.
- الفهارس الفريدة للنطاق والدومين موجودة.
- لا توجد نطاقات مركزية مكررة في الفحص الحالي.
- دفتر الأستاذ والقيود المنشورة محميان بسياسة immutability/reversal.
- تخزين مستندات الطلاب خاص ومعزول.
- دور `edupro_app` الآمن موجود.

### المخاطر

- الدور المستخدم حاليًا يتجاوز RLS.
- الاتصال المركزي المميز لا يملك URL منفصلًا في البيئة الحالية.
- اتصال PostgreSQL المميز يستخدم `ssl: { rejectUnauthorized: false }` ما لم يكن SSL معطّلًا بالكامل؛ هذا يشفر النقل لكنه لا يتحقق من شهادة الخادم كما ينبغي.
- fallback إلى ملف/متصفح ما يزال موجودًا في طبقات قديمة كثيرة؛ بعض المسارات تفشل مغلقًا، وبعضها ما يزال متاحًا في workspace الداخلي.
- JSONB snapshot لكل مدرسة في الامتحانات وHR والمخزون والمالية يقلل عدد الجداول لكنه يرفع مشاكل الحجم والتزامن والتقارير الجزئية.
- error handler ينسب الخطأ غير المصادق افتراضيًا إلى `school_1` عند محاولة تسجيله، ما قد يلوث التدقيق أو يخلق نسبة خاطئة.

---

## 8. الأمن

### نقاط قوة

- bearer token يتحقق عبر Supabase، والهوية والمدرسة والفرع والدور والصلاحيات تُشتق من الخادم.
- محاولات تحديد مدرسة مختلفة من الطلب تُرفض في المسارات الحساسة.
- endpoints المحمية أعادت 401 فعلًا دون رمز.
- auth endpoints عليها rate limit.
- الرسائل التقنية تُخفى عن المستخدم غير الإداري في error handler.
- migrations تحتوي RLS/FORCE RLS ودورًا غير bypass وقيود immutability.

### مخاطر إضافية

- لا يوجد rate limiter عام لواجهات API أو AI أو التقارير؛ التحكم الحالي يعتمد على المصادقة والصلاحية فقط.
- rate limiter الخاص بالمصادقة في الذاكرة وعلى مستوى process؛ لا يوفر حماية موزعة عند تعدد النسخ.
- endpoint العام `/api/supabase/status` يكشف جزءًا من عنوان المشروع، ويعتبر وجود متغيرين «verified/ready» دون فحص اتصال في نفس endpoint.
- refresh token في Web Storage وليس HttpOnly cookie أو ذاكرة محمية.
- CSP وframeguard معطلان.
- مسارات طباعة متعددة تعتمد document.write.
- خادم التحكم المركزي يقبل TLS دون تحقق شهادة.
- Gemini يستقبل سياقًا قد يحتوي PII دون طبقة redaction واضحة.

---

## 9. الأداء وقابلية التوسع

- `server.ts` يقارب 416KB ويحتوي أكثر من مئة endpoint في ملف واحد.
- أكبر المكونات التشغيلية بين 100KB و573KB؛ هذا يضعف قابلية المراجعة والاختبار والعزل.
- endpoint `GET /api/students` يضع كل طلبات القراءة في queue واحدة على مستوى العملية. هذا يمنع تعارض UnitOfWork، لكنه يسبب head-of-line blocking بين مدارس مختلفة ويحد التوسع الأفقي داخل كل process.
- snapshot JSONB الكامل يعني أن تعديل عنصر صغير قد يقرأ/يتحقق/يكتب حمولة مدرسة كاملة.
- حد JSON للطلبات 2MB؛ وحدات snapshot قد تقترب منه مع نمو البيانات.
- لا توجد نتيجة load test حقيقية في هذه المراجعة؛ شاشة اختبار التحمل لا تعد اختبارًا.
- يوجد pooling واتصال readiness وبعض فهارس مركبة، وهي نقاط إيجابية.

---

## 10. جودة الكود والمعمارية

وثيقة `ARCHITECTURE.md` تنص على SOA وDDD وSOLID وفصل الطبقات وعدم اتصال UI مباشرة بقاعدة البيانات. التنفيذ يحقق ذلك في بعض نطاقات شؤون الطلاب والقبول والمالية، لكنه لا يحققه على مستوى التطبيق كله:

- `App.tsx` يجمع المصادقة والتنقل والحالة والبيانات التجريبية والعمليات المالية والحضور وأولياء الأمور والنوافذ.
- `server.ts` يجمع كل نطاقات API تقريبًا.
- مكتبة ونقل يتصلان من UI إلى Supabase مباشرة.
- 2,499 ظهورًا للكلمة `any` في TypeScript/TSX.
- `tsconfig.json` لا يفعّل `strict` أو `noImplicitAny`.
- توجد طبقات قديمة وحديثة متوازية ومصادر حقيقة متنافسة.
- ملفات certification كثيرة جدًا وتقدم أحيانًا ادعاءات «معتمد/Zero Error» لا تمثل الحالة التشغيلية المثبتة.

---

## 11. الاختبارات وضمان الجودة

### الإيجابيات

- 845 اختبارًا ناجحًا تغطي عددًا كبيرًا من العقود والسياسات والمنطق النطاقي.
- توجد اختبارات جيدة للفشل المغلق، الجلسة، العزل، التزامن، وبعض قواعد المالية والامتحانات.
- TypeScript يمر دون أخطاء.

### القيود

- تم إحصاء نحو 2,006 استخدامات لـ `toContain(...)` داخل الاختبارات، مقابل 43 استدعاء render و122 تفاعل fireEvent/userEvent تقريبًا. هذا يوضح أن نسبة كبيرة من الاختبارات تتحقق من وجود نصوص في المصدر، لا من سلوك التطبيق.
- لا توجد بنية Playwright أو Cypress للاختبار الشامل.
- Puppeteer موجود كاعتمادية لكنه غير مستخدم في مجموعة الاختبارات الحالية.
- لا يوجد دليل تغطية حديث في هذه المراجعة.
- لا توجد جولة browser + API + DB كاملة لكل دور وصلاحية.
- نجاح الاختبارات لا يكشف رسائل النجاح الوهمية لأنها قد تكون هي نفسها العقد النصي المختبر.

---

## 12. تجربة الاستخدام والوصول والهوية البصرية

- شاشة الدخول تعمل بصريًا بالعربية وRTL، لكن `<html lang="en">`، ما يضر قارئات الشاشة ومحركات البحث.
- عنوان الصفحة ما يزال `My Google AI Studio App` بدل SchoolForManus.
- رابط favicon مكرر مرتين.
- README ما يزال قالب Google AI Studio عام باسم الحزمة `react-example@0.0.0`.
- 36 ملف مكون يستخدم `select-none`، وظهر 99 استخدامًا؛ منع تحديد النص واسع أكثر من الحاجة.
- توجد أزرار أيقونية بلا accessible name في بعض الشاشات.
- توجد استجابة responsive وفواصل sm/md/lg واستخدام overflow للجداول، لكن لم يتم تنفيذ اختبار بصري شامل لكل شاشة وحجم.
- اعتماد كثير من التقارير على popup/print يجعلها حساسة لحجب النوافذ ويعطي تجربة غير موحدة.
- صورة مستخدم من نطاق خارجي تخلق اعتمادًا وطلبًا خارجيًا غير ضروري.

---

## 13. الاعتماديات والتشغيل والتوثيق

- توجد ثلاثة ملفات lock: `bun.lock`, `package-lock.json`, `pnpm-lock.yaml`.
- التثبيت الحالي من نمط pnpm؛ لذلك `npm ls` يعرض نتائج extraneous/missing مضللة، بينما `pnpm list` ناجح.
- تعدد مديري الحزم يزيد احتمال اختلاف build بين المطور وCI والإنتاج.
- ثغرتا `qs` بدرجة Moderate موجودتان في شجرة الإنتاج الحالية.
- أوامر `clean` و`restore` في package.json موجهة لبيئة Unix/Git وقد تكون غير ملائمة أو مدمرة على Windows، كما أن `restore` يتخلص من تغييرات ملف امتحانات كامل.
- `dist` غير متتبع Git؛ الحزمة التي تعمل محليًا مؤرخة 2 سبتمبر 2026، والمصدر الحالي فُحص منفصلًا.
- يوجد توثيق ضخم (866 ملف Markdown)، لكن كثرة تقارير الاعتماد والشهادات لا تعوض غياب Runbook موحد ودليل نشر حديث وOpenAPI مولد.

---

## 14. ترتيب الأولويات المقترح — دون تنفيذ

### أولوية فورية قبل أي إطلاق

1. اعتبار اتصال `BYPASSRLS` مانع إطلاق، وفصل data plane عن control plane وتفعيل بوابة البيئة الصارمة.
2. منع كل رسالة نجاح لا يقابلها commit/إرسال/حفظ مركزي، أو حجب الوحدة بالكامل حتى تكتمل.
3. إغلاق سلسلة XSS/token: CSP، frame protection، تخزين الجلسة، وتعقيم جميع print templates.
4. إيقاف اعتماد وحدات الأكاديمي والحضور وأولياء الأمور والزي والأصول والإعدادات كوظائف إنتاجية في وضعها الحالي.
5. وضع سياسة AI للبيانات الشخصية، الحقول المسموحة، الإخفاء، الموافقة، الاحتفاظ والتدقيق الخارجي.

### أولوية عالية لاحقة

6. توفير API خادم canonical للمكتبة والنقل والحضور وأولياء الأمور والزي والأصول والإعدادات.
7. إعادة خريطة التنقل بحيث يستطيع المستخدم الوصول إلى كل وحدة مسموحة له.
8. توحيد كل الاستدعاءات على `authenticatedRequest` أو طبقة واحدة تدير refresh/401/409/idempotency.
9. تفكيك الملفات العملاقة وتقليل snapshot الكامل إلى نماذج/عمليات أكثر دقة.
10. إضافة E2E متعدد الأدوار والمستأجرين يشمل المتصفح والخادم والقاعدة.

### أولوية جودة وتشغيل

11. توحيد مدير الحزم وإزالة ملفات lock المتنافسة.
12. معالجة ثغرات الاعتماديات وتثبيت SCA في CI.
13. تفعيل TypeScript strict تدريجيًا وتقليل `any`.
14. تحديث README والعنوان واللغة وبيانات المنتج.
15. استبدال شهادات «Zero Error» بنتائج آلية مرتبطة ببناء واختبارات وبيئة محددة.

---

## 15. الحكم النهائي لكل فئة

### يمكن البناء عليه

- المخطط الحي والمهاجرات.
- الإدارة المركزية الأساسية.
- المصادقة والتحقق الخادمي والصلاحيات.
- شؤون الطلاب والقبول.
- أساس المالية ودفتر الأستاذ.
- حفظ الامتحانات وHR والمخزون بإصدارات وتدقيق.

### يحتاج استكمالًا قبل الاعتماد

- الحسابات بكامل دورة الإقفال والتقارير المعتمدة.
- رسوم الطلاب والتكامل الكامل مع GL.
- الامتحانات وHR والمخزون تحت حمل وتزامن حقيقيين.
- التنقل وتجربة المستخدم والوصول.
- المراقبة والنسخ الاحتياطي والتعافي والنشر.

### لا ينبغي تقديمه حاليًا كوظيفة إنتاجية مكتملة

- الشؤون الأكاديمية CRUD.
- الحضور.
- مراسلة أولياء الأمور.
- الزي المدرسي.
- الأصول المستقلة.
- الإعدادات العامة والعملة كمصدر مركزي.
- المكتبة كاملة الدورة.
- النقل بربطه الحالي.
- اختبار التحمل وشهادات A/A+ داخل الواجهة.
- المراجعة العامة.

---

## 16. حالة التقرير عند تاريخ المراجعة

عند إعداد هذا التقرير في 3 سبتمبر 2026 أُجريت المراجعة الأصلية للقراءة فقط، ولم تُعدّل ملفات المصدر أو migrations أو إعدادات أو سجلات قاعدة البيانات، ولم تُنفذ أي عملية إنشاء/تحديث/حذف داخل البرنامج.

## 17. سجل الإصلاحات اللاحقة

بعد اعتماد بدء الإصلاح بنداً بنداً، نُفذت دفعة أولى منفصلة عن المراجعة الأصلية، وتشمل:

- إغلاق جاهزية الخادم افتراضياً حتى يثبت اتصال البيانات دوراً غير مميز وغير متجاوز لـ RLS، مع فصل اتصال الإدارة المركزية في البيئات الإنتاجية.
- تفعيل CSP و`X-Frame-Options` افتراضياً، وإخفاء حالة Supabase عن العامة وربطها بصلاحية الإدارة وفحص الجاهزية.
- منع رسائل النجاح الوهمية في الحضور، الرسائل، الزي، العملة، الأكاديمي، مركز الصحة، اختبار التحمل، الفهارس، الأرشفة، Redis والنسخ المقروءة.
- جعل «تذكرني» غير مفعّل افتراضياً، ومعالجة HTML قبل طباعة الخطط الأكاديمية.
- إيقاف التنبؤ المالي حتى يتوفر مصدر رسوم مركزي موثق وسياسة خصوصية معتمدة، ومنع الاعتماد على بيانات يرسلها المتصفح.
- إضافة تقييد المدرسة إلى قراءات وتحديثات سجل النقل وتحويل أسماء حقول النقل بين صيغة قاعدة البيانات وصيغة الواجهة.
- تصحيح بحث الطلاب في الشريط العلوي وربطه بقائمة المدرسة الفعلية وربط زر الإعدادات بالمسار الموجود، وتصحيح مفتاح جلسة مركز الصحة إلى مدير الجلسة الموثوق.
- تشديد عزل سجل الحضور على مستوى `school_id`، وحسابات المكتبة والزي عبر علاقة الطالب بالمدرسة في القراءة والتعديل والحذف، مع تحويل الحقول بين صيغة الواجهة وصيغة قاعدة البيانات.
- منع أزرار تغيير حالة الحضور من تعديل حالة React محلياً دون حفظ مركزي؛ أصبحت تعطي رسالة توقف واضحة بدلاً من تغيير غير قابل للتدقيق.
- تشديد حذف كتب المكتبة وسجل الكتب المستعارة على نطاق المدرسة، وتحسين البحث عند غياب عنوان أو مؤلف.
- إزالة صور Unsplash الافتراضية من بطاقات الطلاب؛ عند غياب صورة موثوقة تعرض الواجهة الحرف الأول فقط لتقليل الاتصالات الخارجية وتسريب بيانات التصفح.
- ربط أزرار المظهر والتنبيهات والمساعدة في شاشة الصلاحيات برسائل حالة صادقة بدلاً من أزرار بلا handler، وإزالة صورة المستخدم الخارجية الثابتة.
- منع الإدارة والصلاحيات من تحميل قوائم المستخدمين والأدوار وسجل التدقيق من `localStorage` عند تفعيل الوضع المركزي؛ أصبح المصدر المركزي الموثوق شرطاً لعرضها.
- إزالة معرفات المدارس الافتراضية (`school_1`/`sch_1`) من مسارات الصحة والزي حتى لا تُنفذ عملية على مستأجر تخميني عند غياب مدرسة محددة.
- إيقاف عمليات القياسات والصرف وتعديل الأسعار وإضافة الموردين والأصناف في وحدة الزي عند غياب مسار مخزون مركزي موثوق، ومنع محرك المحاكاة المحلي من إصدار نجاح إنتاجي.
- منع تحميل بيانات العرض التجريبية (المعلمون، الموظفون، الفواتير، المخزون، الحضور، الهيكل الأكاديمي، الزي والدرجات) في الوضع المركزي؛ تبدأ هذه الوحدات فارغة إلى أن تُغذّى من API موثوق.
- إزالة معرفات الطلاب والأصناف التجريبية من الاختيارات الافتراضية في البوابات القديمة، بحيث لا يبدأ أي إجراء على سجل تخميني.
- ربط قراءة سياق الشؤون الأكاديمية في الوضع المركزي بـ`/api/academic/context` الموثق، مع تحديث السنة والهيكل من الخادم وإظهار تحذير عند فشل المصدر بدلاً من الاعتماد على قيم واجهة ثابتة.
- إضافة محدد معدل عام لواجهات `/api` بحدود ذاكرة مضبوطة، مع إبقاء فحوص الصحة والمصادقة على سياساتها المنفصلة.
- ضبط دورة تحميل السياق الأكاديمي حتى لا تعيد طلب الخادم مع كل إعادة رسم للواجهة.
- استكمال API مركزي للمكتبة باستخدام UnitOfWork وسياق RLS الموثوق: قراءة/إنشاء/تعديل/حذف الكتب، قراءة الإعارات، إنشاء الإعارة وإغلاقها، مع فحص النسخ والطلاب وتسجيل audit داخل المعاملة، وربط بوابة المكتبة بالمسارات الجديدة.
- إضافة migration canonical لفهرس المكتبة وسجل الإعارات (`library` و`borrowed_books`) مع مفاتيح المدرسة/المستأجر وفهارس وسياسات RLS؛ لم تُطبق migration على قاعدة البيانات.
- إضافة مسار خادم موثق لقراءة سجل حضور الطلاب (`/api/attendance/records`) مع فلترة المدرسة/الفرع والتاريخ، وربط شاشة الحضور به في الوضع المركزي؛ بقيت الكتابة محجوبة حتى نشر adapter خدمة الحضور مع التدقيق وoutbox.
- إضافة واجهات خادم موثقة لمسارات النقل واشتراكات الطلاب (قراءة/إنشاء/تحديث) مع اشتقاق المدرسة من الجلسة، وتحويل بوابة النقل لاستخدامها في الوضع المركزي.
- إغلاق أزرار إنشاء/حذف/نقل/صيانة/إهلاك/بيع الأصول في الوضع المركزي برسائل واضحة؛ لا تُفتح نماذج توحي بالحفظ قبل ربط دفتر الأصول بالتدقيق والدفتر العام.
- إزالة آخر fallback لمعرف المدرسة `school_1` من معالج أخطاء الخادم؛ أخطاء الطلبات غير الموثقة تُسجل تقنياً مع `traceId` دون اختلاق مستأجر أو مستخدم للتدقيق.
- استكمال تعريف Render بمتغيرات الإنتاج المطلوبة (`DATABASE_URL`, `PLATFORM_ADMIN_DATABASE_URL`, الدور المتوقع، SSL، JWT، ومفاتيح الخدمات) مع إبقاء القيم السرية `sync: false` وعدم تضمين أسرار حقيقية.
- إضافة `PUBLIC_APP_URL` ومفتاحي التكامل الاختياريين إلى تعريف Render كأسرار غير متزامنة، مع إبقاء التنبؤ المالي معطلاً افتراضياً.
- إضافة أمر `npm run production:gate` لفحص بيئة staging/production، الاتصالات، الدور المقيّد، SSL، HTTPS، وتعطيل التنبؤ المالي دون كشف الأسرار أو طباعتها.
- فحص بوابة الإصدار محلياً؛ النتيجة `success: false` كما يجب لأن البيئة المحلية لا تحتوي إعدادات إنتاج مكتملة. تم اختبار مسار النجاح بقيم اختبارية غير سرية، ونجح الفحص.
- ربط بوابة الإصدار بأمر بناء Render؛ أي نشر staging/production بإعداد ناقص يتوقف قبل بناء الحزمة.
- إضافة تصحيح مركزي لسجل حضور قائم عبر `PATCH /api/attendance/records/:id` بمعاملة واحدة وفحص إصدار متزامن وربط المستخدم الموثوق وتسجيل `audit_events`، وربط أزرار الشاشة به عند توفر سجل canonical.
- توحيد عقد دور اتصال Staging مع النشر: إضافة migration منفصلة تنشئ `edupro_staging_app` كدور دخول غير مميز وغير متجاوز لـRLS، يرث سطح منح `edupro_app` المراجع، وتوسيع دوال اشتقاق سياق المستأجر لقبول دورَي الإنتاج وStaging؛ لم تُطبق migration على قاعدة البيانات.
- اجتياز أمر البناء الكامل `npm run build` للواجهة وحزمة الخادم، ثم إزالة تحذيرات `import.meta` من إخراج CommonJS بفصل كشف البيئة عن Vite؛ بقيت تحذيرات حجم chunks فقط.

شملت الدفعة الملفات المصدرية ذات الصلة فقط. لم تُنفذ أي كتابة على قاعدة البيانات أثناء هذه الدفعة. تحقق `npm run lint` وبناء الواجهة والخادم بنجاح، وأزيلت تحذيرات `import.meta` من إخراج CommonJS.

## 18. حالة الإغلاق التنفيذية — 3 سبتمبر 2026

- حالة المصدر: مغلق من ناحية الإصلاحات البرمجية المنفذة في هذه الدفعة؛ `npm run lint` و`npm run build:spa` و`npm run build` و`git diff --check` ناجحة.
- حالة بوابة الإصدار: تعمل كمانع نشر؛ تفشل محليًا عند غياب أسرار الإنتاج، ونجح مسار اختبار Staging بقيم وهمية غير سرية.
- تم التحقق كذلك من مسار Production الاختباري غير السري بنجاح مع الدور المتوقع `edupro_app`.
- إضافة قالب `.env.staging.example` غير سري ليكون قائمة تحقق موحدة لفريق النشر إلى جانب قالب Production.
- تشديد بوابة الإصدار لتتحقق من أن اسم مستخدم `DATABASE_URL` يطابق الدور المقيد المتوقع، وأن اتصال الإدارة منفصل ولا يستخدم دور البيانات؛ أي اتصال `postgres` أو اتصال مكرر يُرفض قبل البناء.
- استكمال سجل الصلاحيات لعقود المكتبة والنقل (`library:*`, `library:borrow:*`, `buses:insert/edit`) وإضافة تعريفَي الدورين `librarian` و`transport_manager` حتى لا تُرفض مسارات الوحدة بسبب صلاحية غير مسجلة.
- إغلاق دورة المكتبة المركزية برمجيًا: API للكتالوج والإعارات مع UnitOfWork وRLS وسجل تدقيق وفحص توافر النسخ ومنع حذف كتاب ذي إعارة نشطة، وربط شاشة المكتبة بالقراءة والإنشاء والإرجاع المركزي.
- إغلاق كتالوج الزي مركزيًا: إضافة `GET/POST/PATCH/DELETE /api/uniform/items` مع نطاق المدرسة المشتق من الجلسة، حفظ الحقول التفصيلية داخل `uniforms.data`، منع الإدخال غير الصالح، وتسجيل `audit_events`، وربط شاشة الزي بالتحميل والإضافة المركزيين. أضيفت كذلك طبقة متغيرات ومبيعات ومخزون canonical؛ أما دورة الشراء التفصيلية والموردون والقياسات فتظل ضمن snapshot/مساراتها الحالية إلى حين ربطها بعقودها المتخصصة.
- إغلاق الأصول الثابتة ومبيعات/مخزون الزي: أضيفت `fixed_assets`, `fixed_asset_events`, `uniform_item_variants`, `uniform_sales`, `uniform_sale_lines`, `uniform_stock_movements` مع RLS، append-only للحركات، ومنع الرصيد السالب، وربط الرسملة والإهلاك والصيانة والبيع والشطب وحركات المخزون بقيود ERP canonical داخل معاملة واحدة.
- حالة قاعدة البيانات: غير مغلقة تشغيليًا بعد؛ يجب تطبيق المهاجرات بالترتيب، بما فيها `202609031000_library_canonical.sql` و`202609031100_staging_app_role.sql` و`202609031200_fixed_assets_canonical.sql` و`202609031300_uniform_inventory_sales.sql`، عبر عملية قاعدة البيانات المعتمدة.
- حالة النشر: غير معتمدة بعد؛ يجب تزويد Render بالقيم السرية الفعلية، والتأكد من أن `DATABASE_URL` يتصل فعليًا بدور `edupro_staging_app` في Staging أو `edupro_app` في Production، ثم التقاط دليل `/api/ready` وفحص الهوية عبر جلسة مصادقة حقيقية.
- لا توجد عملية تلقائية لإنشاء الأدوار أو تشغيل المهاجرات عند إقلاع الخادم، ولا يجوز اعتبار نجاح البناء وحده اعتمادًا إنتاجيًا.
- الفحص الآمن لملف `.env` المحلي أثبت أنه غير صالح للتشغيل: لا توجد بيئة معلنة، ولا `SUPABASE_SERVICE_ROLE_KEY` أو `PLATFORM_ADMIN_DATABASE_URL` أو `JWT_SECRET` أو SSL/URL عام، واسم مستخدم `DATABASE_URL` هو `postgres...` وليس دور التطبيق المقيد؛ لذلك تم منع استخدامه تشغيليًا.
- تمت إزالة تحذيرات `import.meta` من حزمة CommonJS عبر فصل كشف بيئة الخادم عن عقد Vite؛ المتبقي فقط تحذيرات حجم chunks الكبيرة في Vite، وهي غير مانعة للبناء.
- اجتازت مجموعة Vitest الكاملة 323 ملف اختبار و845 اختبارًا؛ المحاولة الأولى داخل العزل كانت محجوبة من محلل esbuild (`Access is denied`) ولم تُحتسب، ثم أُعيد التشغيل بصلاحية تنفيذ مناسبة.
- بعد إضافة مسار كتالوج الزي، أعيد تشغيل `npm run lint` و`npm run build` ومجموعة Vitest الكاملة: `323` ملفًا و`845` اختبارًا ناجحًا. بقيت تحذيرات حجم chunks في Vite فقط، وهي غير مانعة للبناء.
- أُغلقت طبقة الأصول الثابتة ومخزون/مبيعات الزي برمجيًا: migrations canonical جديدة للأصول وحركاتها ومتغيرات الزي وحركات المخزون والمبيعات، مع RLS وحركات append-only، ومسارات خادم ذرّية تربط كل رسملة/إهلاك/بيع/شطب/صيانة وحركة مخزون وبيع بقيد ERP canonical وتدقيق، وربط شاشة الأصول وبيع الزي بهذه المسارات.
- أسماء ترحيلات الدفعة: `202609031200_fixed_assets_canonical.sql` و`202609031300_uniform_inventory_sales.sql`؛ لم تُطبق على أي قاعدة بيانات خارجية.
