# تقرير جاهزية UAT لخمسة وثلاثين مدرسة

التاريخ: 30 أغسطس 2026  
البيئة المفحوصة: SchoolForManus UAT / Staging  
الحالة: **جاهزية بيانات UAT والعزل: ناجحة — الإغلاق الإنتاجي الكامل: غير معتمد بعد**

## ما تم تنفيذه والتحقق منه

- تطبيق والتحقق من مخطط الموارد البشرية الكانوني `hr_database` مع RLS وقيود النطاق.
- إنشاء الجداول الكانونية للنقل والزي: `buses` و`uniforms` و`student_transportation` و`student_uniform_accounts`، مع مفاتيح نطاق المدرسة وRLS و`FORCE ROW LEVEL SECURITY`.
- تفعيل RLS على `roles` و`permissions` و`role_permissions` و`user_roles`، وتفعيل FORCE RLS لموارد HR.
- تضييق سياسات قراءة المدارس والمستخدمين لمنع انتحال مدرسة عبر إعدادات الطلب.
- إضافة واجهة API مركزية للمدارس (`GET/POST/PATCH /api/admin/central/schools`) محمية بصلاحية `PLATFORM_ADMIN`، وتكتب المدرسة والفرع والسجلات الأولية للموارد البشرية والمخزون والمالية داخل معاملة PostgreSQL واحدة.
- ربط شاشة إدارة المدارس بالإضافة والتعديل والتجميد والأرشفة والحذف الآمن بالمصدر المركزي؛ لا يحدث تعديل محلي عند فشل API.
- تشغيل UAT محكوم بالبادئة `UAT35-` وإنشاء 35 مدرسة و35 فرعاً و70 طالباً و35 سجل HR و35 سجلاً للنقل و35 للزي، ثم إصلاح روابط الأدوار والتحقق منها.

## نتيجة بنية البيانات

| النطاق | النتيجة | الدليل |
|---|---|---|
| المدارس والفروع والطلاب | ناجح | 35 مدرسة، 35 فرعاً، 70 طالباً |
| الموارد البشرية | ناجح | 35 سجل `hr_database` مع وظائف وموظفين وعقود اختبار |
| المخزون والمشتريات | ناجح | 35 سجل `inventory_database` مرتبط بكل مدرسة |
| الحسابات | ناجح | 35 سجل `financial_portal_snapshots` مرتبط بكل مدرسة |
| النقل والترحيل | ناجح | 35 حافلة و35 علاقة نقل طالب |
| الزي المدرسي | ناجح | 35 صنف زي و35 حساب زي طالب |
| الأدوار والصلاحيات | ناجح | RLS مفعل وروابط `user_roles`/`role_permissions` موجودة في UAT |
| التدقيق | ناجح | المصدر الكانوني المتحقق هو `audit_events` وليس جدولاً وهمياً باسم `audit_logs` |

## نتيجة العزل الحي

اجتاز `scripts/verify-uat35-isolation.ts` الاختبار بقيمة `passed: true`:

- اختُبرت مدرستان مختلفتان في نفس المستأجر.
- ظهرت لكل مدرسة سجلاتها الخاصة فقط في الطلاب وHR والمخزون والمالية والنقل والزي والأدوار.
- عدد السجلات الأجنبية الظاهرة كان صفراً في جميع الجداول المفحوصة.
- قراءة المدارس والفروع عند محاولة تبديل إعدادات الطلب يدوياً فشلت مغلقة؛ وهذا يثبت مقاومة انتحال النطاق. يلزم لاحقاً حسابا Auth منفصلان للمدارس الـ35 لإثبات القراءة الإيجابية لكل مدرسة بحسابها الحقيقي.

## حالة الإدارة المركزية

| الشاشة/النطاق | الحالة الحالية | قرار الاعتماد |
|---|---|---|
| إدارة المدارس | CRUD تشغيلي مركزي للإنشاء والتعديل والحالة والأرشفة | معتمد ضمن المسار المنفذ |
| تهيئة المدرسة | ذرية: مدرسة + فرع + HR + مخزون + مالية | معتمد |
| إدارة المستخدمين والهوية | بعض عمليات الإنشاء الحساسة تفشل بأمان، لكن إدارة الحسابات الكاملة تحتاج API هوية مركزي | غير معتمد للإغلاق الكامل |
| الوحدات والاشتراكات والفروع | توجد أجزاء محلية/غير مربوطة بعد | غير معتمد |
| النسخ الاحتياطية وصحة النظام | لا يجوز اعتبار مؤقتات الواجهة نسخة أو فحصاً فعلياً | غير معتمد حتى ربط مزود التشغيل |
| التدقيق والتنبيهات | بعض العرض محلي، ومسارات البث المركزي غير متاحة | غير معتمد كخدمة مركزية كاملة |

## الاختبارات البرمجية

- TypeScript: ناجح.
- `git diff --check`: ناجح.
- البناء الإنتاجي للواجهة وتجميع الخادم: ناجح، مع تحذيرات `import.meta` الموجودة مسبقاً في مخرجات CJS.
- اختبارات العقود المستهدفة: 3 ملفات، 7 اختبارات، كلها ناجحة.
- فحص UAT والعزل الحي: ناجح.

## الحكم المهني

تم إغلاق فجوة قاعدة البيانات والعزل الخاصة بوحدات المدارس وHR والمخزون والحسابات والنقل والزي، وتم تحويل إدارة المدارس إلى مسار مركزي فعلي. لا يصح إعلان الإغلاق الإنتاجي الكامل لكل الإدارة المركزية بعد، لأن إدارة المستخدمين والاشتراكات والفروع والنسخ الاحتياطية والتنبيهات ما زالت تحتاج خدمات خادم حقيقية أو يجب تعطيل أزرارها إلى حين الربط. بيانات UAT قابلة للحذف عبر مشغل `cleanup` المحمي بمعرف التشغيل والتأكيد الصريح، ولم تُحذف الآن لأن طلب الاختبار أبقاها متاحة للمراجعة.
