# محضر إغلاق ما تبقى من جاهزية التسليم — 2 سبتمبر 2026

## الحكم التنفيذي

تم إغلاق المتبقي القابل للإغلاق داخل المستودع برمجيًا وبناءً واختبارًا. لا يصح إعلان الإغلاق التجاري النهائي أو إغلاق الوحدة المركزية بشكل مطلق قبل إرفاق أدلة خارجية حية؛ لأن البريد والنسخ الاحتياطي والنطاق التجاري وخطة الاستضافة واختبار عزل حسابين مستقلين تتطلب وصولًا أو اعتمادًا من مالك البنية.

الحالة الحالية:

- **الإغلاق البرمجي:** `CLOSED`
- **الإغلاق التشغيلي الحي:** `PARTIALLY CLOSED — LIVE READINESS VERIFIED`
- **التسليم التجاري النهائي:** `PENDING OWNER ACCEPTANCE`

## ما تم إغلاقه

| البند | الدليل | الحالة |
| --- | --- | --- |
| استيراد/تصدير الجداول | استبدال `xlsx` بـ `ExcelJS 4.4.0`، مع دعم XLSX وCSV والتحقق من الخلايا والصيغ الخطرة | مغلق |
| الامتدادات غير المدعومة | إزالة مسارات `.xls` القديمة من وحدات الاستيراد ذات الصلة | مغلق |
| حماية واجهات الدخول والتشخيص | محدِّد طلبات داخلي محدود الذاكرة، مع حد أقصى للمفاتيح، رؤوس معيارية، و429/503 بفشل آمن | مغلق |
| مخاطر التبعيات | `npm audit --omit=dev`: صفر؛ `pnpm audit --prod`: صفر. تم تثبيت `body-parser 1.20.6` و`protobufjs 7.6.5` و`nanoid 3.3.18` و`uuid 14.0.1` عبر القفلين | مغلق |
| سلسلة البناء | `render.yaml` يستخدم `npm ci --include=dev` ويجعل `/api/ready` مسار صحة Render | مغلق داخل المصدر |
| جودة الكود | `pnpm lint` ناجح، وبناء SPA والخادم ناجحان | مغلق |

## نتائج التحقق

- مجموعة الاختبارات النهائية السابقة: `319` ملفًا و`833` اختبارًا ناجحة؛ أضيفت بعدها اختبارات السعة والأمان الخاصة بالتشغيل الصناعي.
- ضمنها اختبار المحدِّد الجديد: `1` ملف و`4` اختبارات ناجحة.
- اختبار السعة متعدد المدارس: `1` ملف و`3` اختبارات ناجحة، واختبار أمان مشغل الحمل: `1` ملف و`2` اختبارين ناجحين.
- لا تُعرض أسرار أو مفاتيح في هذا المحضر.
- واجهات النسخ/التعافي غير المتصلة بمزود خارجي ما زالت تفشل بأمان ولا تسجل نجاحًا اصطناعيًا.

## الدليل الحي المسجل

- المستودع المنشور: `salafe10-ship-it/schoolfor-manus`، الفرع `master`، الالتزام النهائي `d316098` (ويتضمن `30bc8ff`).
- خدمة Render: `schoolfor-manus-staging`، والخدمة تعرض `Deploy succeeded | Live`.
- عنوان الخدمة: `https://schoolfor-manus-staging.onrender.com`.
- نشر الالتزامين `30bc8ff` و`d316098` نجح؛ وسجل التشغيل يثبت تشغيل الخادم، اتصال PostgreSQL، وربط Supabase.
- `/api/health`: HTTP `200`، والحالة `healthy` مع `tenantIsolationMode: Row-Level Security (RLS Active)`.
- `/api/ready`: HTTP `200`، والحالة `READY`، وقاعدة البيانات `CONNECTED`، و`ready: true`.
- تم تحديث Health Check في Render إلى `/api/ready`، ثم نجح نشر الإعداد الجديد.
- تشغيل GitHub Actions `Staging uptime #1` على الالتزام `d316098` نجح خلال `7s`، والجدولة الدورية كل خمس دقائق مفعّلة.

### دليل الإنتاج المستقل

- خدمة الإنتاج المنفصلة: `https://edupro-school-erp.onrender.com`.
- `/api/ready`: HTTP `200`، والحالة `READY`، وقاعدة البيانات `CONNECTED`، وقناة البيانات تعمل بدور `edupro_app` المقيد بـRLS.
- فحص قاعدة الإنتاج المباشر: `57` جدولًا ذا نطاق مدرسي، `57` منها مفروض عليه RLS، و`0` مخالفات؛ دور التطبيق غير متجاوز لـRLS، وحاوية مستندات الطلاب خاصة.
- حساب الإدارة المركزي المعتمد موجود ومؤكد البريد ونشط ويحمل دور `platformadmin`. اختبار الدخول الحالي فشل بسبب عدم تطابق كلمة المرور، لا بسبب الدور أو عزل البيانات.
- SMTP المخصص في مشروع الإنتاج غير مفعّل بعد؛ لذلك لا تُرسل رسالة استعادة قبل إكمال إعداد مزود بريد موثوق واختبار التسليم.

## معيار النمو متعدد المدارس

- معيار القبول البرمجي هو `5,000` سياق مدرسة مستقل و`15,000` مستخدم موزعين عليها، وليس مدرستين فقط.
- المنصة تستخدم Pool مشتركاً محدداً الاتصالات ولا تنشئ Pool لكل مدرسة، مع عزل `tenant/school/branch/year` في السياق والـRLS.
- أضيف اختبار عقدة يشغّل `5,000` سياقاً مستقلاً بالتوازي، ويتحقق من عدم تصادم المدارس ومن عزل ذاكرة التخزين المؤقت.
- يوجد اختبار حمل مركزي اختياري في `scripts/load-test-central.ts` بإعداد افتراضي `5,000` مدرسة و`15,000` مستخدم، وتزامن تنفيذ محدود قابل للضبط؛ وضعه الافتراضي الآن `plan` بلا كتابة، وأي إنشاء بيانات صناعية يتطلب تأكيد `SCHOOLFORMANUS_5000_LOADTEST_APPLY`؛ لم تُنشأ بيانات صناعية في الخدمة الحية الحالية حفاظاً على المدرستين الفعليتين.
- وجود مدرستين فعليتين الآن لا يعني أن النظام محصور بهما؛ لكنه يعني أن شهادة حمل `5,000` مدرسة تحتاج تشغيلًا منفصلًا على Staging مع بيانات صناعية معتمدة، ثم تنظيفها، قبل اعتماد رقم أداء تجاري.

## البنود التي تحتاج دليلًا خارجيًا قبل ختم التسليم

1. نشر النسخة الحالية على Render ثم حفظ نتيجة حية لـ `/api/health` و`/api/ready` وتسجيل الدخول — **تم إثبات نشر الإنتاج والجاهزية؛ اختبار الدخول ينتظر من المالك تعيين كلمة مرور صحيحة للحساب المركزي**.
2. اعتماد SMTP وإثبات وصول رسالة استعادة كلمة المرور ورابطها.
3. تنفيذ نسخة إنتاج مشفرة واختبار Restore موثق مع سياسة احتفاظ.
4. ربط النطاق التجاري وإثبات DNS وSSL عبر فحص خارجي موثوق.
5. اعتماد خطة Render أو قبول قيد الخطة المجانية كتابةً.
6. تنفيذ اختبار عزل حي للمدرستين الحاليتين، ثم شهادة حمل منفصلة حتى `5,000` مدرسة و`15,000` مستخدم صناعي على Staging؛ لا تُخلط شهادة العزل مع رقم السعة.
7. قناة المراقبة الحالية مضافة في `.github/workflows/staging-uptime.yml`؛ تفحص الجاهزية كل خمس دقائق وتفتح/تغلق Issue عند الفشل والتعافي. اعتماد قناة مناوبة/تجميع سجلات خارج GitHub Actions يبقى قرارًا تشغيليًا منفصلًا.

## قرار التسليم

لا يتم تغيير الحالة إلى `UNIT CLOSED` أو `COMMERCIAL HANDOVER APPROVED` اعتمادًا على تغييرات المصدر وحدها. بعد إرفاق الأدلة السبعة أعلاه، يعاد تشغيل فحص التسليم وتُحدّث الحالة في محضر مستقل؛ أما سجل الوحدات العام فيبقى `IN PROGRESS` لأن وحدات أخرى ما زالت تحمل بوابات إغلاق منفصلة.
