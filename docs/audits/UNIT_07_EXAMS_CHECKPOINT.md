# نقطة استئناف وحدة 07 — الامتحانات والنتائج

**آخر تحديث:** 2026-08-26  
**حالة الوحدة:** `IN PROGRESS — NOT CLOSED`  
**الفرع:** `codex/checkpoint-2026-08-23`

هذه الوثيقة نقطة استئناف قابلة للاستخدام بعد انقطاع الكهرباء أو توقف المهمة. لا تمثل تقرير إغلاق ولا تصريح تسليم.

## حالة الحفظ والاستئناف الحالية (2026-08-26)

- آخر حالة كود محفوظة محلياً وفي GitHub على الفرع `codex/checkpoint-2026-08-23` هي commit `89dbc0d`؛ يتبعها الآن تحديث سجل الإغلاق هذا.
- آخر commit يتضمن توحيد أمر Render القابل لإعادة الإنشاء: `npm ci && npm run build`، مع إبقاء النشر التلقائي عند كل commit.
- الخدمة الحية محفوظة في Render داخل مشروع `My project` وبيئة `Staging` باسم `schoolfor-manus-staging`، والرابط العام: `https://schoolfor-manus-staging.onrender.com`.
- تم التحقق من فتح شاشة الدخول، فحص صحة الخدمة الداخلي، إعادة البناء، ونجاح Auto Deploy من GitHub إلى Render. هذه الأدلة تثبت التشغيل السحابي فقط ولا تغيّر قرار إغلاق وحدة الامتحانات.
- لاستئناف العمل بعد انقطاع الكهرباء: افتح هذا الملف أولاً، ثم نفّذ `git status --short --branch`، واستخدم آخر commit محفوظ قبل متابعة بند «نقطة المتابعة الحالية».

## ما ثبت حتى الآن

- العمل السابق محفوظ في مساحة العمل وملتزم في GitHub؛ لا توجد عملية merge/rebase أو قفل Git عالق.
- خط الأساس قبل الجولة الحالية نجح: TypeScript، و23 ملف اختبار امتحانات/56 اختباراً، وبناء الواجهة والخادم.
- أضيف محرك نتائج موحد يطبق سياسة التقريب وحد النجاح والحد الأدنى، ويمنع ترتيب النتيجة غير المكتملة.
- أصلحت شاشة دليل التشغيل وترتيب القائمة الجانبية وتقارير الشعبة والقاعة وخصائص الوصول الأساسية.
- أضيف كشف حقيقي لتداخل الفترات الزمنية للصفوف والقاعات ـ بما فيها القاعات المجزأة ـ والمراقبين.
- أضيفت بوابة نطاق مركزية تمنع اعتماد النتائج قبل اعتماد الجدول، وإكمال الجلوس والدرجات ومراجعة المواد وإغلاق التظلمات.
- وثيقة المقارنة الرسمية مع عشرة أنظمة محفوظة في `docs/exams/exams-competitive-benchmark-2026-08-26.md`.

## دليل الاختبار الحي — UAT (2026-08-26)

- تم تشغيل دورة `الاختبارات النهائية` في مدرسة `SchoolForManus UAT-B School` من خلال الواجهة الحية والمصدر المركزي المتصل.
- تحققت التهيئة: 6 مواد، 11 صفاً/شعبة، قاعتان بسعة إجمالية 40، و31 طالباً موزعين جميعاً مع أرقام جلوس فريدة، وجدول سليم بلا تعارض حرج.
- رُصدت وحُفظت 186 درجة اختبارية (31 طالباً × 6 مواد)، ثم أعيد تحميل الشاشة وثبتت القيم القادمة من المصدر المركزي.
- غطت بيانات الاختبار الكسور النصفية وسياسة النجاح بالمادة: 30 ناجحاً بمجموع `487/600` (81%)، وطالب اختبار واحد راسب بمجموع `453/600` (75.5%) بسبب درجة الرياضيات `45/100`.
- أكدت المراجعة اكتمال الرصد 100% وعدم وجود درجات فوق النهاية العظمى؛ وأكدت التقارير نسبة نجاح 97% ومعدلاً عاماً 80.8%.
- تظل الشهادات المعتمدة والطباعة المعتمدة محجوبتين كما ينبغي حتى اعتماد النتائج وإنشاء أرشيف خادم غير قابل للتعديل. لم يُنفذ اعتماد أو إقفال نهائي أثناء الاختبار.
- تم توحيد قيمة مرشح نوع الامتحان في شاشات الرصد مع قيمة الدورة (`الاختبارات النهائية`)؛ نجح فحص TypeScript و25 اختبار واجهة متعلقاً بالامتحانات بعد هذا التصحيح.

## تدقيق حي موسع وقرار الإقفال (2026-08-26)

- فُتحت الشاشات الست عشرة في وحدة الامتحانات من المتصفح الحي أمام المستخدم؛ بقي المصدر المركزي `متصل` ولم يظهر خطأ واجهة أو تحذير Console أثناء التنقل أو الاختبارات الآمنة.
- اختُبرت فعلياً مزامنة الفصول والهيكل، التوزيع الآلي للمراقبين، تجهيز الجدول وتكوينه ومراجعته وتحسينه، تحميل الدرجات وإعادة حسابها ومراجعتها، معالجة النتائج، التقارير، معاينة الشهادات، فحص الحوكمة، نموذج التظلم (فتح/إلغاء دون إنشاء طلب)، النسخة الاحتياطية، ودليل التشغيل. كما تم التنقل بين تبويبات الجدول الأربعة بنجاح.
- الفحص الذاتي الحي أكد: تهيئة الدورة سليمة، المقاعد والقاعات سليمة، اكتمال الدرجات سليم؛ وأظهر بصدق نقطتين معلقتين هما `الجدول غير معتمد بعد` و`لا يوجد اعتماد نتائج مع أرشيف خادم مكتمل`.
- أُعيد تنظيم القائمة الجانبية حيًا إلى مراحل واضحة: `البدء والتهيئة`، `اللجان والجدولة`، `الرصد والنتائج`، `المراجعة والإصدار`، و`الإدارة والمساعدة`، مع الحفاظ على ترتيب دورة العمل وروابط الأزرار.
- وُحّد تصميم شاشتي `توزيع الطلاب` و`الشهادات وكشوف الدرجات`: بطاقات وجداول وحقول داكنة بحدود ذهبية متسقة مع هوية الكنترول؛ لم تتغير صلاحيات الحفظ أو الحذف أو الاعتماد.
- مرّت حزمة الاختبار البرمجية الحالية لوحدة الامتحانات: 26 ملفاً و77 اختباراً. كما اكتمل بناء الإنتاج بنجاح. تحذيرات البناء المتبقية تخص وحدات عامة (`import.meta` في مخرجات CJS) وليست عطل بناء لوحدة الامتحانات.
- لم يُختبر زر الاستعادة لأنه يعيد تحميل الحالة المركزية وقد يستبدل عمل UAT الحالي؛ ولم تُنفذ أزرار الاعتماد أو التوقيع أو الإرسال الرسمي لأنها تغيّر الحالة الأكاديمية أو تنشئ تواصلاً حقيقياً. هذه ضوابط مقصودة وليست نجاحاً مثبتاً لمسار الإقفال.
- **قرار الإنتاج في بداية الجولة: `NOT CLOSED / BLOCKED`.** كان بنك الأسئلة وتشغيل الامتحان الإلكتروني غير موصولين بالواجهة والمصدر المركزي. هذا القرار استُكمل بدليل الجولة الحية أدناه، لكنه لا يتحول إلى `UNIT CLOSED` قبل اكتمال بوابات الأدوار والتصدير وثلاث دورات.
- يلزم قبل الإقفال النهائي: إثبات EX-P0-02 وEX-P0-10 وEX-P0-12، وإتمام اعتماد الجدول والنتائج والأرشيف في دورة UAT منفصلة، ثم نجاح ثلاث دورات حيّة متتابعة بلا خلل Sev-1 أو Sev-2.

## جولة الإغلاق الحالية — الامتحان الإلكتروني (2026-08-26)

- تم ربط `AssessmentLifecycleEngine` بواجهة تشغيل فعلية داخل `ExamsResultsModule` تحت زر `بنك الأسئلة والامتحان الإلكتروني`، وبالحفظ المركزي المتفائل داخل `exams_database` في الحقل `exams_assessment_state`.
- أصبحت الوظائف التالية قابلة للتنفيذ والحفظ والاستئناف: إنشاء سؤال وتصنيفه، تعديل المسودة، نسخ/تجميد الإصدارات، التفعيل والأرشفة، إنشاء نموذج، انتقالات `draft → review → approved → scheduled → open → closed → marking → results_approved → published → archived`، بدء المحاولة، الحفظ التلقائي، التسليم، التصحيح الآلي، التصحيح اليدوي للمقالي/الملف، الإنهاء، وقراءة بوابة النشر.
- تفعيل إصدار سؤال جديد يؤرشف الإصدار السابق مع إبقاء النماذج السابقة مرتبطة بنسختها الثابتة؛ لا يمكن أن يغيّر تعديل بنك الأسئلة امتحاناً منشوراً.
- أضيفت اختبارات مسار مخصصة للحفظ بعد إعادة التحميل، منع الانتقال غير القانوني، منع النشر قبل تصحيح المقالي، والتدرج الكامل حتى النشر. آخر نتيجة للمجموعة الكاملة: **294 ملفاً و745 اختباراً ناجحاً**، و`npm run lint` و`npm run build` ناجحان.
- أُغلقت فجوة التنفيذ البرمجية الخاصة بالتصدير داخل مسار الامتحان الإلكتروني: CSV وXLSX وتقرير RTL للطباعة/الحفظ PDF، مع مؤقت محاولة محفوظ، تحقق خادمي من حالة المسار، ومنع إضافة محاولة جديدة لطالب غير مؤهل. كما أضيفت قيود الواجهة والخادم لانتقالات المعتمد، لكن هذا لا يثبت وحده عزل كل الأدوار في بيئة عميل متعددة المستخدمين.
- لم تُعتبر هذه الجولة إغلاقاً إنتاجياً نهائياً بعد: يلزم إثبات التصدير من Render، واختبار أدوار الطالب/المصحح/المعتمد والعزل الخادمي الكامل، وتشغيل ثلاث دورات حيّة متتابعة. لذلك تبقى الحالة أدناه `IN PROGRESS — NOT CLOSED` حتى اكتمال الدليل.

## دليل UAT الحي بعد ربط معاملات Render (2026-08-26)

- بعد ظهور خطأ `معاملة قراءة الامتحانات غير متاحة` في أول فحص، ثبتت سجلات Render أن `Supabase linked!` موجودة لكن `DATABASE_URL/DIRECT_URL` لم يكن مضبوطاً؛ نُقل `DATABASE_URL` إلى متغير سري في Render ثم نجح النشر وفحص الصحة.
- من الرابط الحي داخل بيئة `UAT-B` ظهر المصدر المركزي `متصل`. أُنشئ سؤال اختيار واحد، حُفظ كمسودة، فُعّل، ثم أُنشئ نموذج `فحص حي لإغلاق وحدة الامتحانات` بمدة 15 دقيقة.
- نُفذت الدورة الحية كاملة: `مراجعة → معتمد → مجدول → مفتوح → مغلق → قيد التصحيح → نتائج معتمدة → منشور → مؤرشف`، مع بدء محاولة، حفظ إجابة، تسليم، تصحيح آلي، إنهاء التصحيح، والنتيجة `1 من 1`.
- بعد إعادة تحميل الرابط والعودة إلى الشاشة، استُعيدت من المصدر المركزي القيم نفسها: سؤال `1`، امتحان `1`، محاولة `1`، والحالة `مؤرشف`. هذا يثبت الحفظ والاستئناف الحي لهذا المسار في UAT-B.
- أُضيف في commit التالي قيد مطابقة معرف الطالب مع قائمة الطلاب الرسمية قبل بدء أي محاولة جديدة؛ بيانات محاولة UAT القديمة أُنشئت قبل هذا القيد ولا تُستخدم كدليل على صلاحية معرف غير رسمي.
- في commit `89dbc0d` أضيف تحقق الخادم من حالة الامتحان الإلكتروني، ومنع المحاولات الجديدة غير الموجودة في قائمة الطلاب المؤهلين، ومؤقت البدء/الانتهاء المحفوظ، وأدوات تصدير CSV/XLSX وتقرير RTL القابل للحفظ PDF. لم تُثبت هذه الإضافات على Render بعد في هذه النقطة من السجل.

## نقطة المتابعة الحالية

1. تنفيذ اختبار حي مصادق من رابط Render لمسار التصدير الجديد، مع فحص ملف CSV وXLSX والطباعة/الحفظ PDF بصرياً ودلالياً.
2. اختبار أدوار الطالب/المصحح/المراقب/المعتمد والعزل الأفقي بين المدارس، مع إثبات رفض الخادم لا إخفاء الزر فقط.
3. تطبيق/التحقق من مخطط قاعدة البيانات المنفصل إذا تطلبت بيئة الإنتاج فصل الأسئلة والمحاولات عن JSON المركزي.
4. تشغيل ثلاث دورات حيّة متتابعة من الإنشاء حتى النشر وإعادة التحميل بلا Sev-1 أو Sev-2، على بيانات اختبار جديدة ومؤهلة رسمياً.
5. توحيد كل الشاشات والطباعة والتصدير مع محرك النتائج نفسه، ثم تحديث سجل الإغلاق وتقرير الوحدة النهائي وفق الأدلة؛ لا يجوز وضع `UNIT CLOSED` ما دام أي بند P0 غير مثبت.

## قاعدة الاستئناف

- لا تستخدم `git reset` أو `git checkout --` لأن تغييرات الامتحانات وأعمال وحدات أخرى غير ملتزمة.
- ابدأ بـ `git status --short` ثم `npm run lint` واختبارات `src/__tests__/exam*`.
- اعتبر البيانات الحية والاعتماد والأرشيف حقائق خاصة بالمدرسة لا تُستنتج من الحالة الافتراضية في الواجهة.
