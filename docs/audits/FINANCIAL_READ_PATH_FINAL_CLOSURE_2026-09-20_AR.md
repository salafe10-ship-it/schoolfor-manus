# تقرير بوابة إغلاق مسار القراءة المالية

**التاريخ:** 2026-09-20  
**النطاق:** الحسابات العامة، المصدر المالي، القيود، مراكز التكلفة، والربط الحي مع وحدة الرسوم  
**قاعدة السلامة:** لم يتم تنفيذ أي INSERT أو UPDATE أو DELETE مالي، ولم يتم فتح التحصيل أو الترحيل.

## القرار التنفيذي

**NOT READY — WRITE MUST REMAIN LOCKED**

## الأدلة المحلية

| البوابة | النتيجة | الدليل |
|---|---|---|
| TypeScript lint | PASS | `npm run lint` |
| Route integrity | PASS | 66 مسارًا، بلا أخطاء |
| SPA build | PASS | `npm run build:spa`، تم تحويل 3044 وحدة |
| Vitest | BLOCKED | Vitest/esbuild يفشل قبل بدء الاختبارات عند قراءة إعداد Vitest بسبب Access denied؛ لا يوجد فشل assertions مثبت |
| Deployment | PASS | آخر نشر Cloudflare ظاهر في GitHub ناجح |
| Production uptime | PASS | فحوص الإنتاج وstaging الظاهرة في GitHub ناجحة |

## الاختبار الحي المرئي

تم فتح البرنامج المنشور واختبار وحدة الحسابات العامة من خلال الواجهة:

- المدرسة الحالية: مدارس الأسرة الحديثة.
- الفرع: الفرع الرئيسي.
- العام الدراسي: 2026-2027.
- المصدر الظاهر: **متصل للقراءة فقط**.
- الواجهة تمنع الترحيل والإقفال حتى اعتماد خدمة دفتر الأستاذ.
- شجرة الحسابات تعرض 42 حسابًا.
- الذمم المدينة الظاهرة: `601,405.00`.
- إيرادات الرسوم الظاهرة: `601,405.00`.
- القيود المعروضة: 4 قيود، وكل قيد متوازن مدينًا ودائنًا.
- حالة القيود: `posted` و`canonical`، مع منع التعديل المباشر.
- مراكز التكلفة المعروضة من المصدر المركزي للمدرسة الحالية:
  - `ST-KG` — رياض الأطفال والتمهيدي.
  - `ST-PRI` — المرحلة الابتدائية.
  - `ST-MID` — المرحلة المتوسطة.
  - `ST-HIGH` — المرحلة الثانوية.

## بوابات لم تُغلق بعد

| البوابة | الحالة | السبب |
|---|---|---|
| Source of Truth | BLOCKED | الواجهة تؤكد اتصال snapshot للقراءة، لكنها تعرض صراحة أن snapshot غير متحقق |
| Tenant Isolation | BLOCKED | لم يُنفذ اختبار رفض cross-tenant حي بمدرسة ثانية |
| Stages | PASS جزئي | ظهرت المراحل من الدليل المركزي، لكن لم يُثبت في هذا الاختبار مسار DB → API → UI لكل مرحلة |
| Cost Centers | PASS جزئي | ظهرت المراكز من المصدر المركزي، لكن لم يُثبت كامل الربط السجلي مع المرحلة في استعلام قابل لإعادة الاختبار |
| Operational Context | PASS جزئي | الفرع والسنة والمدرسة ظاهرة كهوية موثوقة، دون تقرير endpoint كامل |
| Ledger Enrichment | PASS جزئي | القيود متوازنة ومنفصلة عن snapshot، لكن سيناريو فشل enrichment لم يُختبر حيًا |
| No Fallback | BLOCKED | يلزم دليل سلبي حي يثبت عدم استخدام fallback عند تعطل المصدر |
| Fail-Closed | PASS | الكتابة والترحيل والإقفال محجوبة، وشجرة الحسابات للقراءة فقط |
| Financial consistency | BLOCKED | السند 255 معتمد لكنه غير مرحل؛ لذلك بقاء `paidAmount` دون تغيير والمدفوعات 0 قد يكون صحيحًا حسب العقد الحالي، لكن دورة الانتقال Approved → Posted لم تُثبت في بيئة اختبار معزولة |

### تحديث الاختبارات 2026-09-20

- تم تثبيت `--configLoader native` في أمر `test` لمعالجة مشكلة محمّل إعدادات Vitest.
- الاختبار الكامل: **341 ملفًا / 939 اختبارًا / 0 فشل — PASS**.
- اختبارات العزل والقراءة المركزية المركزة: **7 ملفات / 46 اختبارًا / 0 فشل — PASS**.
- بوابة `FIN-READ-SOT-TENANT-001`: **1 ملف / 4 اختبارات / 0 فشل — PASS**؛ وتثبت مسار القراءة الموثق، نطاق `tenant_id` و`school_id`، فصل POST ذي الصلاحية، والفشل المغلق عند غياب المصدر أو السياق الموثوق.
- بعد اكتشاف زر الترحيل القابل للتنفيذ مرئيًا، أضيف قفل خادمي fail-closed قبل معالجات `/api/financial`: القراءات فقط مسموحة، وكل طلب مالي غير قرائي يعيد `423 FINANCIAL_WRITES_LOCKED`.
- إعادة الاختبار الشامل بعد القفل الخادمي: **344 ملفًا / 946 اختبارًا / 0 فشل — PASS**.
- لم يُنفذ أي INSERT أو UPDATE أو DELETE مالي أثناء هذه الجولة.

### بوابة الإنتاج المحلية

`npm run production:gate` لم يعتمد البيئة المحلية لأن أسرار الإنتاج غير موجودة فيها:

- `EDUPRO_ENVIRONMENT` غير محدد كـ staging/production.
- `SUPABASE_SERVICE_ROLE_KEY` غير موجود أو placeholder.
- `JWT_SECRET` غير موجود أو placeholder.
- دور قاعدة البيانات المتوقع غير مثبت.
- التحقق من شهادة SSL غير مفعّل محليًا.

هذه نتيجة **BLOCKED بيئيًا** وليست مبررًا لإضافة أسرار الإنتاج أو تجاوز البوابة. الاعتماد التشغيلي يجب أن يتم من بيئة Cloudflare/GitHub المحمية.

## الملاحظة الحرجة

ظهرت ظاهريًا حالة تحتاج إثباتًا، وليست مخالفة محاسبية مثبتة بعد:

- سند قبض معتمد وغير مرحل: `255.00`.
- المطالبة المقابلة في التقرير: `255.00` بحالة «غير مسدد».
- المدفوعات المتراكمة في التقرير: `0.00`.
- الكود يحدّث `paidAmount` و`remainingAmount` ويربط القيد فقط داخل مسار `POST /api/financial/receipts/manual-settle`، أي بعد الترحيل الخادمي.
- يلزم اختبار دورة معزولة لإثبات أن الاعتماد وحده لا يغيّر الرصيد، وأن الترحيل الناجح يغيّره مرة واحدة ويربطه بالقيد.

هذا يمنع اعتماد التكامل المحاسبي حتى يتم إثبات دورة الحالة والعلاقة بين المطالبة، سند القبض، القيد، ورصيد الطالب من المصدر نفسه.

## القرار النهائي

**Financial Write: مقفلة.**  
**التحصيل والترحيل والإقفال: غير مسموحة.**  
**الخطوة التالية:** إغلاق إثبات المصدر الحقيقي والعزل وربط سند القبض بالمطالبة والقيد قبل أي فتح تدريجي للكتابة.

## بوابة القراءة الحية المتقاطعة FIN-LIVE-READ-CROSS-MODULE-001

قرار المستشار: **البوابة البرمجية PASS، أما البوابة الحية المتقاطعة فما زالت BLOCKED حتى يكتمل تتبع سجل واحد عبر الوحدات والعزل الفعلي.**

الدليل الحي الحالي من البرنامج:

- الحسابات العامة تعرض المصدر المركزي للقراءة فقط، و4 قيود مرحلة ومتوازنة؛ وتظهر حركة مطالبة بقيمة `255 LYD` كقيد مطالبة، لا كسند قبض مرحل.
- شاشة سندات القبض تعرض أن الترحيل غير متاح، والمعاينة فقط، ومجموع إيراد الصندوق `0 LYD`، ولا يظهر فيها سند قبض مرحل.
- هذا يدعم مبدئيًا سلسلة `Approved + Not Posted`، لكنه لا يكفي وحده لإثبات نفس `receipt_id` و`claim_id` و`tenant_id` عبر الرسوم والحسابات العامة ولوحة المؤشرات، ولا يثبت رفض Tenant آخر من الواجهة الحية.
- لوحة المؤشرات المرئية تعرض الإيرادات `601,405.00` والمصروفات `0.00` والسيولة `0.00`، وتضع مؤشرات التوازن والاعتماد كـ«غير متحقق — قراءة فقط». هذا ليس نجاحًا محاسبيًا مصطنعًا؛ بل يعني أن الواجهة تمنع اعتماد نتيجة نهائية من snapshot غير معتمد، ويجب إبقاؤه كذلك حتى يثبت المصدر الحي.
- أثناء الفحص المرئي ظهرت بوابة «ترحيل قيد اليومية» قابلة للتنفيذ في شاشة سند قبض معتمد، رغم أن المصدر المركزي معلن قراءة فقط. عولج ذلك محليًا بإضافة قفل موحد `financialWritesLocked = true` يحرس كل أزرار الطفرات، مع رسالة مرئية صريحة «المصدر المالي متصل للقراءة فقط — الحفظ والترحيل مقفلان». اختبار واجهة القفل: **2/2 PASS**، واختبار المصدر والعزل: **4/4 PASS**. يلزم نشر هذا الإصلاح ثم إعادة التحقق من المتصفح قبل اعتبار البوابة مغلقة.

لذلك لم أفتح الكتابة ولم أعتبر الوحدة مغلقة بعد. المطلوب التالي المحدد هو التقاط Evidence حي لسند 255 نفسه عبر الوحدات الخمس، مع المعرّفات والمصدر والحالة والقيم الناتجة، ثم اختبار Tenant A مقابل Tenant B دون أي INSERT/UPDATE/DELETE.

### دليل النشر والتحقق الحي

- تم دفع الالتزام `24592de` إلى `master`.
- Cloudflare Production Deploy **Run 71: Success**؛ تقرير GitHub: `344` ملف اختبار و`946` اختبارًا ناجحًا.
- بعد النشر وإعادة تحميل البرنامج، ظهرت في شاشة سندات القبض الحية الرسالة: **«المصدر المالي متصل للقراءة فقط — الحفظ والترحيل مقفلان»**.
- زر **«ترحيل قيد اليومية»** ظاهر **معطلًا**، وكذلك جديد/حفظ/تعديل/اعتماد/عكس/حذف.
- السند `RV-STUD-2ba319c8-df45-4329-a3c3-588e634ac8dd` ظاهر `معتمد ماليًا` و`رُحّل بواسطة: —`؛ أي اعتماد بلا ترحيل، دون أثر كتابة جديد.

### تحقق مرئي للإصدار 5a036aa

- بوابة الحسابات العامة فتحت إنتاجيًا على المدرسة والفرع والعام الدراسي الصحيحين: `مدارس الأسرة الحديثة` / `الفرع الرئيسي` / `2026-2027`.
- شجرة الحسابات: **42 حسابًا** مع أرصدة معروضة، وأزرار جديد/تعديل/تعطيل/استيراد مقفلة قراءة فقط.
- القيود اليومية: **4 قيود**، ميزان المراجعة متطابق، **0 مسودات**، وكل القيود المعروضة `posted` محمية كـ `canonical`.
- زر شاشة القيود والتعديل على القيود المرحلة معطلان، والرسالة المركزية تؤكد أن الترحيل والإقفال غير متاحين حتى اعتماد خدمة دفتر الأستاذ.
- حالة النشر المرصودة: الالتزام `5a036aa` يشغل Run 72 للنشر وRun 171 للتحقق الإنتاجي؛ لم يُفتح قفل الكتابة.
