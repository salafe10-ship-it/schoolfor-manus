# نقطة توقف تدقيق الإدارة المركزية — 2026-08-30

## الغرض

حفظ حالة التدقيق التشغيلي للإدارة المركزية حتى يمكن استئناف العمل بعد انقطاع التيار من النقطة نفسها، دون اعتبار الوحدة مغلقة إنتاجيًا قبل اكتمال الاختبارات والبناء والنشر.

## الحالة الحالية

- المشروع: `schoolfor-manus`.
- الفرع المحلي: `codex/checkpoint-2026-08-23`.
- آخر نسخة منشورة: `895cb24` على `master`، وحالتها Live على Render.
- لا توجد تعديلات مصدرية محلية غير ملتزمة؛ الملف المؤقت المحلي الخاص بـ Supabase CLI غير متتبع ولم يُرفع.
- لا يوجد في هذه النقطة أي تغيير مقصود على المستودع الآخر الذي طلب المستخدم إبقاءه كما هو.

## ما تم إنجازه في هذا التدقيق

- مراجعة شاشات الإدارة المركزية في `src/components/super-admin` شاشةً شاشةً.
- إضافة مسار قراءة سجل التدقيق المركزي من الجداول الموثقة `audit_events` و`audit_access_events` في `server.ts`.
- تحويل شاشة سجل التدقيق إلى القراءة من المصدر المركزي، ومنع الحذف المحلي أو اختلاق سجل محو.
- إيقاف جلسات المحاكاة المحلية في شاشة الولوج الفني؛ لا تُعلن الجلسة نجاحًا دون إصدار خادم مركزي وتدقيق موثق.
- إزالة التخزين المحلي من شاشة التحديثات، وعدم اختلاق إصدارات أو عمليات نشر محلية.
- تعديل لوحة الإدارة المركزية لتعرض المؤشرات غير المتصلة بصيغة «غير متحقق» بدل أرقام تشغيلية مصطنعة.
- بدء تنظيف مركز العمليات من البث الحي العشوائي وبيانات الوحدات المحلية التجريبية.

## تحديث الاستئناف — 2026-08-31

- اكتمل تنظيف `SuperAdminOperationsCenter.tsx`: أزيلت النسب والتواريخ وأشرطة التقدم الافتراضية، وأصبحت حالة الوحدات «غير متحقق» عند غياب الدليل المركزي.
- أغلقت شاشة النسخ الاحتياطي والصحة والمنصة المطورة عرض اللقطات أو القياسات أو إعدادات المزود غير الموثقة؛ الأزرار غير الموصولة تشرح سبب عدم التنفيذ، ومسارات API نفسها تعيد فشلًا آمناً حتى تهيئة موصل موثوق.
- أصبح غلاف الإدارة المركزية يحمّل المستأجرين والمدارس والفروع والأعداد المركزية عند الدخول، وتوصل إجراءات الاشتراك والنطاق الفرعي في مركز العمليات بالمسار الكانوني مع حماية التحديثات الجزئية من مسح بيانات الملف.
- أُضيفت شاشة «إدارة المستأجرين» بعقود إنشاء وتعديل وتعليق وأرشفة حقيقية، مرتبطة بجدولي `tenants` و`subscriptions`، مع اختيار المستأجر عند إنشاء المدرسة وإدارة المدارس والفروع والمستخدمين عبر النطاق المركزي.
- أُضيف ترحيل `202608311000_tenant_lifecycle_guard.sql` لمنع دخول المستخدم العادي عند تعليق أو أرشفة المستأجر، وأصبحت أرشفة المستأجر تشمل المدارس والفروع والمستخدمين والاشتراكات.
- أصبحت إجراءات التجديد والتمديد وتغيير الباقة في شاشة الاشتراكات تحدّث `subscriptions` الكانوني للمستأجر، كما تحمل شاشة المدارس تاريخ الاشتراك وحد المقاعد من المصدر نفسه.
- أصبح تأسيس مدرسة أو فرع محصوراً في مستأجر غير مؤرشف وغير موقوف، مع فهارس فريدة للنطاقات الحية ومنع ربط نطاق مستخدم مسبقاً.
- أزيلت آخر قيمة افتراضية لمدة الاشتراك من محرر المدارس؛ لا تظهر مدة أو حالة إلا من السجل الكانوني، وتبقى السعة التخزينية «غير متحقق» حتى توفير موصل التخزين.
- أصبح إيقاف الترخيص من شاشة الاشتراكات يغيّر حالة المستأجر الكانونية كاملةً، فتظهر المدارس التابعة محجوبة ولا تظل مدرسة شقيقة نشطة بالخطأ؛ كما يدعم فحص النطاق السجلات القديمة التي تحتوي `customDomain` فقط.
- أضيف قياس حقيقي محدود لاتصال PostgreSQL المركزي وزمن الاستجابة في شاشة الصحة، مع كشف انجراف مخطط الإدارة المركزية وعدّ الكائنات المفقودة، وإبقاء مؤشرات CPU/الشبكة/النسخ غير متحققة؛ وأصبحت أرشفة مدرسة واحدة تتسلسل إلى فروعها وهوياتها مع استعادة آمنة كمعلّقة للمراجعة، وكذلك استعادة المستأجر المؤرشف كمعلّق مع مدارس معلّقة وفروع مغلقة ومستخدمين معلّقين دون إعادة اشتراك ملغى تلقائياً.
- طُبّق نمط بصري موحد مع بوابة المدارس: عاجي/بني/ذهبي، بطاقات مستديرة، حالة دليل واضحة، وشارات لا تدعي SSL أو مراقبة غير مثبتة.
- حُدّثت اختبارات العقود المتقادمة لتتحقق من المصدر المركزي قبل إعلان النجاح، ومن عدم وجود بيانات محلية مصطنعة.
- تحقق ناجح: `npm run lint`، و`npm run build`، و`git diff --check`، وVitest الكامل (313 ملفات / 815 اختباراً) بعد تحديث عقود نطاق الإدارة المركزية.
- فحص القراءة الآمن السابق للترحيل أثبت اتصال PostgreSQL ووجود الجداول المركزية، وكشف أن فهارس النطاقات وحارس حالة المستأجر لم يكونا مطبقين حينها؛ كانت هوية الاتصال `postgres`. استُخدمت هذه النتيجة كحاجز قبل الترحيل.
- قناة Supabase المحلية لا تحتوي CLI أو `SUPABASE_ACCESS_TOKEN`؛ لذلك لم تُنفذ `db push`. بعد تأكيد المستخدم للمشروع الصحيح، نُفذت الترحيلات المحددة عبر اتصال PostgreSQL المباشر فقط.
- فحص قناة الاتصال الأولي أظهر مرجعًا مختلفًا عن مشروع Staging الموثق في سجل البنية (`vjcjscqgmijgzagshsca`)؛ وبعد تأكيد المستخدم، ثبت أن مشروع Production المقصود هو `wjhraxvxvvthxqlpyohh`، ولذلك لم يُستخدم أي اتصال قبل مطابقة الوجهة.
- أضيف الأمر القراءة فقط `npm run db:verify-central` ليجمع فحص الجداول والفهارس ودوال دورة الحياة وهوية الدور وتعارضات النطاق. قبل الترحيل كشف الفهارس والحراس المفقودة، وبعد الترحيل أكد اكتمال الكائنات وعدم وجود تعارضات، مع بقاء فحص الدور متوقفاً عند `bypassRls=true`.
- بعد اعتماد المستخدم، طُبّقت الترحيلات `202608311000_tenant_lifecycle_guard.sql` و`202608311100_central_domain_uniqueness.sql` على مشروع Production `wjhraxvxvvthxqlpyohh` داخل معاملة واحدة؛ التحقق اللاحق أثبت الفهارس والحراس الأربعة وعدم وجود تعارضات نطاق. بقي فحص الدور غير ناجح لأن الاتصال ما زال `postgres` مع `bypassRls=true`، ولا يوجد دور تطبيق مقيد في المشروع.
- فحص قراءة جدول `supabase_migrations.schema_migrations` بعد استخدام قناة Supabase CLI المصرح بها أكد تسجيل الترحيلين `202608311000` و`202608311100` كـ applied؛ لم يُكتب جدول النظام يدويًا.
- كشف تدقيق العزل الشامل بتاريخ 2026-08-31 وجود **26 جدولًا نطاقيًا** كان RLS فيها معطّلًا، و40 جدولًا نطاقيًا مفعّلًا دون `FORCE RLS`. بعد اعتماد المستخدم، طُبّق `202608311200_strict_school_isolation.sql` على Production وأضاف سياسات tenant/school/branch للجداول المكشوفة وفرض RLS على جميع الجداول النطاقية الـ65، ثم طُبّق `202608311300_scope_policy_role_hardening.sql` لحصر سياسات اللقطات المالية في `authenticated`.
- أُضيف الترحيل `202608311400_restricted_app_role.sql`، وأنشئ دور `edupro_app` بصلاحيات دخول وتشغيل مقيدة و`NOBYPASSRLS`، مع منح الصلاحيات التشغيلية اللازمة فقط. فحص الاتصال المباشر أثبت أن الدور يسجل `rolbypassrls=false` وأن جميع الجداول النطاقية الـ65 محمية.
- فُصلت اتصالات Render: `DATABASE_URL` للتطبيق عبر الدور المقيد، و`PLATFORM_ADMIN_DATABASE_URL` للإدارة المركزية عبر اتصالها المميز. حُفظت القيم في Render دون إدراج أي سر في المستودع أو السجل، ثم اكتمل نشر commit `895cb24` بنجاح.

نسبة الإغلاق داخل نطاق الإدارة المركزية والـschema والعزل المراجع: **100%**. العزل بين المدارس/المستأجرين في قاعدة البيانات: **100% من الجداول النطاقية الـ65 اجتازت فحص RLS + FORCE RLS وسياسة القراءة**. نسبة جاهزية بوابات المدارس الأساسية: **73%** بحسب مصفوفة الوظائف السابقة، ولا تعني فشل العزل؛ وتبقى الموصلات الخارجية الاختيارية غير المهيأة خارج نطاق الإغلاق.
- تحقق حي بعد تبديل الأسرار: `/api/health` و`/api/ready` أعادا HTTP 200 مع `healthy` و`READY` و`database=CONNECTED` على `schoolfor-manus-staging`.

## نقطة التوقف الحالية بعد الاستئناف

اكتملت جولة التدقيق البرمجية وتطبيق ترحيلات الإدارة المركزية والعزل الشامل على مشروع Production المؤكد، كما اكتمل إنشاء الدور المقيد وفصل اتصالات Render وcommit وpush والنشر الحي والتحقق من health/readiness. لا تُعتبر النسخ الاحتياطية والتعافي وRedis ومراقبة البنية وDNS/SSL والانتحال الفعلي موصولة إنتاجيًا حتى توفير موصلاتها الخارجية.

## الأعمال المطلوبة عند الاستئناف

1. توفير الموصلات الخارجية المطلوبة فقط إذا أُريد اعتماد النسخ الاحتياطي والتعافي وRedis ومراقبة البنية وDNS/SSL والانتحال الفعلي.

## قاعدة الإغلاق

لا يُعتمد الإغلاق التشغيلي النهائي ما لم تكن الشاشة أو الزر إما مرتبطًا بمسار مركزي قابل للإثبات، أو مغلقًا بأمان مع بيان عدم توفر الموصل. لا يجوز استخدام أرقام تجريبية أو سجلات مولدة أو رسائل نجاح مؤقتة كدليل تشغيل.
