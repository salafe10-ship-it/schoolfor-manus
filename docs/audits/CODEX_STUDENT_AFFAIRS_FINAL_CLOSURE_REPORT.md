# CODEX — Student Affairs — Final Closure Report

آخر تحديث: `2026-08-31`

## تحديث التنفيذ — 2026-08-31

- أُضيف `CanonicalEnrollmentWorkflowService` وربطته بمسارات النقل والترقية وإعادة القيد الفردية والجماعية.
- العملية الجديدة تتحقق من الهيكل الأكاديمي والسعة والنطاق الموثوق، وتنفذ التعديل داخل معاملة PostgreSQL واحدة مع idempotency و`enrollment_history` و`audit_events` و`outbox_events`.
- أُضيف مسار استعادة الطالب الموقوف `POST /api/students/:id/reinstate` مع تصحيح كانوني موثق للحالة، وأصبحت الواجهة تستخدمه بدل تعديل عام غير كافٍ.
- أُضيف مسار استيراد الطلاب `POST /api/students/import` مع قراءة XLSX/XLS/CSV ومعاينة قبل الحفظ، والتحقق من الحقول، ومفتاح دفعة، ومعاملة PostgreSQL ذرية؛ التسجيلات الفردية تنضم إلى نفس المعاملة، لذلك لا يمكن حفظ جزء من الدفعة.
- أُصلح `scripts/build-server.mjs` لاستخدام مسارات مطلقة، وأصبح `npm run build` ناجحًا على Windows.
- بقيت الوحدة غير مغلقة بسبب الشهادات الرسمية، التخزين الثنائي، ومصدر التخرج الأكاديمي؛ لا تُعلن هذه المسارات نجاحًا وهميًا.

## Q — القرار النهائي

## 🔴 UNIT NOT CLOSED — BLOCKERS REMAIN

تم إغلاق وإثبات مسارات القراءة والحفظ والتحرير والحارس وإعادة التحميل والبحث والطباعة والتصدير والمصادقة والاختبارات. لا يمكن إعلان الوحدة جاهزة للتسليم لأن وظائف مطلوبة في الأمر الأصلي غير منفذة أو غير قابلة للإثبات من النظام الحالي.

## A — النتائج الأصلية

- كانت صلاحية جلسة الطلبات مرتبطة بمسار غير موحد، وكانت طلبات Student Affairs قد تفقد سياق المستأجر في بعض مسارات Node.
- نموذج الطالب كان يعرض أخطاء الحفظ بصورة غير مؤكدة، مع مشكلة في التاريخ والحالة عند إعادة التصيير.
- تحديث ولي الأمر لم يكن يحافظ دائمًا على العلاقة الكانونية العربية/الداخلية.
- مسار القراءة الكانوني كان يعيد تاريخ الميلاد بصيغة قد تُفسر كتاريخ/وقت محلي.
- بعض وظائف النقل والترقية والتخرج كانت موجودة في الواجهة دون خدمة ذرية مكتملة؛ لذلك أصبحت fail-closed بدل ادعاء نجاح.
- مركز المستندات الحالي metadata-only؛ لا توجد طبقة تخزين ثنائية أو تنزيل أو معاينة/OCR.
- لا توجد خدمة شهادة قيد مكتملة؛ أصبح استيراد Excel مسارًا كانونيًا حقيقيًا لكنه ما زال يحتاج UAT حيًا على ملف المؤسسة قبل اعتباره مغلقًا نهائيًا.

## B — الأسباب الجذرية

1. اعتماد جزء من الكود على حالة تشغيلية غير متاحة في ESM/Node بدل `AsyncLocalStorage` الموثوق.
2. عدم توحيد كل الطلبات المباشرة على دورة token/401/refresh المشتركة.
3. عقد بيانات Student Affairs أوسع من العقد الكانوني المتاح فعليًا في قاعدة البيانات.
4. مخطط enrollment الحالي لا يوفر مصدرًا قانونيًا واضحًا للمرحلة والصف والشعبة.
5. مخطط documents يثبت البيانات الوصفية والإصدارات فقط، ويستبعد التخزين الثنائي صراحة.

## C — الإصلاحات المنفذة

- إصلاح بناء خادم esbuild باستخدام مسارات مطلقة.
- إصلاح سياق `TenantContext` في `UnitOfWork` لمسارات Node ESM.
- توحيد طلبات Student Affairs والمستندات على `authenticatedRequest` مع token محلي آمن، refresh واحد وإعادة محاولة واحدة بعد 401.
- إبقاء `getTrustedAccessTokenAsync()` مسؤولًا عن restore/validation الصريح، مع منع أي تجاوز للمصادقة.
- إصلاح حفظ الطالب والتحرير وإظهار أخطاء الخادم وعدم إخفاء فشل الاستمرارية.
- إصلاح تطبيع تاريخ الميلاد إلى `YYYY-MM-DD` والحفاظ على اليوم المحلي.
- إصلاح guardian relationship من العربية إلى القيمة الكانونية والعكس، مع إثبات تحديث الهاتف والإصدار.
- إضافة معاينة طباعة قائمة الطلاب وبطاقة الطالب الرسمية مع RTL وبيانات المدرسة والرقم وتاريخ الميلاد، وإخفاء عناصر التحكم عند الطباعة.
- إضافة سجل خطأ آمن لمسار تصدير XLSX.
- إضافة معاملة استيراد طلاب ذرية بحد 200 صف، ومفاتيح منع تكرار لكل صف، ومعاينة واجهة قبل الاعتماد، وإلغاء كامل عند أي خطأ.
- ربط النقل الجماعي والترقية وإعادة القيد الفردية والجماعية بخدمة قيد ذرية مع السعة وسجل التدقيق وoutbox.
- تحديث اختبارات العقود القديمة لتطابق السلوك الجديد بدل توقع حالة ما قبل الإصلاح.

## D — الملفات المتغيرة

- `scripts/build-server.mjs`
- `server.ts`
- `src/components/StudentAffairsPortal.tsx`
- `src/modules/student-documents/presentation/StudentDocumentsPortal.tsx`
- `src/database/UnitOfWork.ts`
- `src/database/repositories/CanonicalStudentReadRepository.ts`
- `src/utils/auth.ts`
- `src/utils/authenticatedRequest.ts`
- اختبارات Student Affairs والمستندات وauth ذات الصلة.

## E — أثر قاعدة البيانات والمخطط

- أُضيف في بيئة UAT فصل دراسي نشط حتى يمكن اختبار التسجيل الكانوني؛ هذا تغيير إعداد UAT وليس migration إنتاجيًا.
- أُنشئ سجل اختبار UAT للطالب `UAT-CLOSURE-01` وسُجلت عليه عملية تحرير وحفظ حارس للتحقق من الاستمرارية.
- الإثبات المباشر: الاسم أصبح `طالب اختبار إغلاق معدل`، تاريخ الميلاد `2012-05-10`، إصدار الطالب `2`، هاتف ولي الأمر `0500000002`، والعلاقة الداخلية `parent` مع إصدارات الحارس والعلاقة `3`.
- لم تُنفذ migration جديدة للمرحلة/الصف/الشعبة أو التخزين الثنائي أو الشهادات.

## F — API والمصادقة

- القراءة والحفظ والتسجيل والتعديل والتصدير تعمل عبر السياق الموثوق ونطاق tenant/school/branch المستنتج من الجلسة.
- المسارات غير المكتملة للنقل والترقية وإعادة القيد والفصل ترفض التنفيذ بدل الحفظ الجزئي.
- 401 يمر بمحاولة refresh واحدة ثم retry واحد فقط؛ الفشل يتحول إلى AuthenticationRequestError.
- لا يعتمد العميل على `schoolId` المرسل منه لتحديد النطاق الأمني.

## G — تدقيق الأزرار

| الوظيفة | الحالة |
|---|---|
| إضافة طالب | مثبتة حيًا مع حفظ PostgreSQL وإعادة التحميل |
| تعديل الطالب | مثبتة حيًا مع الإصدار والتاريخ |
| تعديل ولي الأمر | مثبتة حيًا مع الهاتف والعلاقة والإصدارات |
| بحث/تصفية/إعادة ضبط | مثبتة حيًا |
| عرض الملف والخط الزمني | مسار API محمي؛ العرض الفارغ/الحالة الصريحة مثبتان |
| طباعة قائمة الطلاب | معاينة RTL وأمر الطباعة مثبتان |
| بطاقة الطالب الرسمية | المعاينة والطباعة مثبتتان؛ رفع الصورة غير متاح |
| تصدير XLSX | إشعار نجاح الخادم وواجهة التنزيل مثبتان؛ لم تُلتقط binary event كملف محلي من أداة Browser |
| مركز المستندات | قراءة metadata ونسخ/سجل وصول فقط؛ binary upload/download غير متاح |
| استيراد Excel | منفذ كمسار XLSX/XLS/CSV ذري بعد المعاينة؛ يحتاج UAT حيًا بملف المؤسسة |
| شهادة القيد | غير منفذة؛ ما زالت موسومة قريبًا |
| النقل الجماعي/الترقية | مثبتان عبر خدمة القيد الذرية مع idempotency وaudit؛ يحتاجان UAT حيًا |
| المرحلة/الصف/الشعبة | غير قابلة للاستخدام لغياب مصدر قانوني معتمد |

## H — تدقيق دورة العمل

- Add → DB → Reload → Search → Edit → Guardian Edit → Reload: `PASS` في UAT.
- التصدير والطباعة: `PASS` على مستوى UI/API المرئي، مع تحفظ binary event للطباعة النظامية النهائية.
- Document metadata: `PARTIAL`; لا توجد فئات UAT مزروعة ولا تخزين ثنائي.
- Bulk transfer/promotion/re-enrollment: `IMPLEMENTED` ذريًا؛ يحتاج UAT حيًا على الهيكل الأكاديمي الفعلي.
- Certificates/image upload/binary documents: `BLOCKED` لغياب backend/schema/storage المطلوب.
- Excel import: `IMPLEMENTED` ذريًا مع preview/rollback/duplicate policy؛ يحتاج UAT حيًا.

## I — الوظائف المفقودة التي تمنع الإغلاق

1. خدمة شهادة قيد رسمية بقالب عربي موقّع وAPI وpreview/print.
2. تخزين ثنائي آمن للملفات مع reference/download/preview وإعادة فتح بعد reload.
3. رفع صورة الطالب وحفظ مرجعها واستخدامها في بطاقة الهوية.
4. مصدر canonical للمرحلة والصف والشعبة وربط enrollment به بصورة نهائية.
5. اختبار UAT حي كامل للاستيراد والنقل والترقية بعد ربط بيانات المؤسسة.

## J — فجوة المقارنة المرجعية

بالمقارنة الوظيفية المطلوبة مع فئات SIS التي تم ذكرها (PowerSchool، Infinite Campus، Skyward، Veracross، Blackbaud K-12، FACTS SIS، Gradelink، Fedena، Classter، YemenSoft)، تتوافر حاليًا أساسيات السجل الكانوني والحارس والتصدير والطباعة والصلاحيات، بينما تبقى فجوات مشتركة في المستندات الثنائية، الشهادات، الاستيراد الجماعي، وبنية enrollment الأكاديمية. هذه ليست شهادة توافق vendor أو benchmark أداء؛ يلزم تنفيذ الوظائف ثم اختبارها على بيانات UAT قبل إعلان التكافؤ.

## K — الأمن والسلامة

- نطاق tenant/school/branch لا يأتي من payload العميل.
- الاستعلام الكانوني يمر عبر تحقق tenant وpermission وcontext.
- التسجيل يستخدم idempotency key، والطلبات المحمية تستخدم Authorization من دورة trusted session.
- المسارات الجماعية غير الجاهزة ترفض ولا تنفذ partial writes.
- لم تُسجل كلمة المرور أو token في هذا التقرير.

## L — UAT المرئي

- تسجيل الدخول بحساب UAT المقدم من المستخدم: `PASS`.
- فتح Dashboard ثم Student Affairs: `PASS`.
- إنشاء وتعديل وإعادة تحميل وبحث الطالب `UAT-CLOSURE-01`: `PASS`.
- تبويب Guardians وعرض العلاقة العربية: `PASS`.
- معاينة بطاقة الهوية ومعاينة قائمة الطباعة RTL: `PASS`.
- تبويب Documents: عرض صريح لعدم توفر binary path، دون ادعاء نجاح.
- تبويب Reports: بطاقة الطباعة تعمل؛ الشهادة ما زالت غير متاحة.

## M — دليل الاستمرارية

تمت مطابقة نتيجة الواجهة بعد reload مع PostgreSQL الكانوني: رقم الطالب، الاسم المعدل، تاريخ الميلاد، هاتف الحارس، العلاقة، وإصدارات optimistic concurrency. لم يعتمد الإثبات على state محلي فقط.

## N — الاختبارات والتحقق

- Full Vitest: `279 files passed / 665 tests passed`.
- `npm run lint`: `PASS`.
- `npm run build`: `PASS`.
- `git diff --check`: `PASS`.
- تحذيرات build الحالية غير مانعة: `import.meta` مع CJS وبعض chunks أكبر من 500KB.

## O — المخاطر المتبقية

- لا يجوز اعتبار الاستيراد أو النقل/الترقية مكتملين نهائيًا قبل UAT على بيانات المؤسسة الفعلية؛ الشهادات والصور والمستندات الثنائية ما زالت غير متاحة.
- لا يجوز اعتبار سجل UAT الاختباري بيانات إنتاجية؛ يجب تنظيفه ضمن إجراء UAT معتمد قبل التسليم.
- يلزم اختبار الطباعة الفعلية على متصفح/طابعة المؤسسة، لا الاكتفاء باستدعاء `window.print()`.
- يلزم اعتماد schema/API قبل إعادة تمكين المرحلة والصف والشعبة.

## P — عناصر غير مثبتة

- التخزين الثنائي وإعادة فتح الملف بعد reload.
- شهادة رسمية موقعة ونسخة مطبوعة نهائية.
- UAT حي للاستيراد والنقل/الترقية على بيانات المؤسسة.
- الشهادة الرسمية الموقعة والتخزين الثنائي ورفع الصورة.
- vendor benchmark فعلي وقياسات أداء مقارنة.

## Checkpoint

هذه النسخة checkpoint إنتاجي أقوى: مسارا الاستيراد والنقل/الترقية أصبحا كانونيين ذريين، لكن القرار العام ما زال `UNIT NOT CLOSED` حتى اكتمال الشهادات/المستندات الثنائية ومصدر التخرج وإثبات UAT حي.
