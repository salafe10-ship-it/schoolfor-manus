# تقرير جاهزية التسليم الإنتاجي — 1 سبتمبر 2026

## الحكم التنفيذي

حالة التطبيق الإنتاجي الحالية: **يعمل وقابل للمراجعة المركزية**.

حالة الفصل: **تم تجهيز إنتاج مستقل ونظيف في مشروع Supabase منفصل، مع انتظار نشر متغيرات Render والتحقق الحي بعده**.

حالة التسليم التجاري النهائي: **غير معتمد بعد** إلى أن تُغلق البنود التشغيلية المفتوحة أدناه، وبالأخص إثبات البريد والنسخ الاحتياطي وDNS/SSL واختبار العزل الحي.

## الأدلة الحالية

| البند | النتيجة |
| --- | --- |
| مصدر الإنتاج | `salafe10-ship-it/schoolfor-manus`, الفرع `master`, الإصدار `72db892` |
| آخر نشر إنتاجي | ناجح، والحالة `Live` |
| رابط الإنتاج | `https://edupro-school-erp.onrender.com` |
| فحص جاهزية Render | مضبوط على `/api/ready`، ويعيد `200` مع `ready: true` بعد اتصال قاعدة البيانات |
| Supabase Auth Site URL | مضبوط على رابط الإنتاج، مع إبقاء رابط Staging ضمن قائمة السماح |
| تسجيل الدخول المركزي | ناجح بصلاحية `SuperAdmin` وإنشاء جلسة موثوقة |
| واجهات الإدارة المركزية | 8/8 واجهات API الأساسية أعادت `200` و`success: true` |
| شاشات الإدارة المركزية | 17/17 شاشة تنقل فُتحت دون خطأ واجهة قاتل |
| الدليل الحالي | 2 مستأجر، 537 مدرسة، 537 فرعًا، 395 مستخدمًا |
| RBAC | 3 أدوار و106 صلاحيات في الكتالوج المركزي |
| الاختبارات البرمجية | TypeScript ناجح؛ 318 ملف اختبار و829 اختبارًا ناجحة |

تمت إعادة فحوص ما بعد نشر `72db892`: الجاهزية `200/ready`، تسجيل الدخول المركزي `200` بصلاحية `SuperAdmin`، وواجهات الإدارة المركزية الأساسية `8/8` ناجحة، وشاشات التنقل المركزية `17/17` دون خطأ قاتل.

## ما أُغلق

- ربط الإنتاج بالمستودع والفرع الصحيحين.
- تصحيح متغيرات Supabase المطلوبة للإنتاج وإعادة النشر.
- التحقق من الدخول المركزي والهوية `SuperAdmin`.
- التحقق من الصحة والاستعداد بعد إعادة التشغيل.
- ضبط Readiness Check في Render على `/api/ready` حتى لا تُعتبر الخدمة جاهزة قبل اتصال قاعدة البيانات.
- ضبط Site URL وRedirect URL للإنتاج في Supabase Auth مع الإبقاء على Staging.
- اختبار القراءة المركزية للمستأجرين والمدارس والفروع والمستخدمين وRBAC والتنبيهات والتدقيق.
- المرور على شاشات الإدارة المركزية الأساسية دون ظهور شاشة خطأ قاتلة.
- إبقاء الوظائف غير الموصولة بمزود خارجي في حالة فشل آمن، دون ادعاء نجاح وهمي.
- حماية استيراد ملفات الطلاب وبنود الرسوم من الملفات الأكبر من 10MB والامتدادات غير المدعومة، مع حد 500 بند رسوم لكل دفعة.

## الفجوات المفتوحة قبل التسليم التجاري

### 1. فصل الإنتاج عن الاختبار — مغلق تقنيًا، بانتظار تحقق النشر الحي

تمت استعادة مشروع Supabase المستقل `bwdjnjbexklsrwqbwzmk` باسم `edupro-school-erp` في Frankfurt. أثبت جرد قراءة فقط أن المستأجرين والمدارس والفروع والمستخدمين وAuth فارغون. أُعيد بناء المخطط من 36 ترحيلًا مع RLS إجباري للجداول المدرسية، دور `edupro_app` غير متجاوز لـRLS، وحاوية مستندات خاصة. تم إنشاء حساب مدير المنصة وربطه بصلاحية `Platform.Admin` فقط.

بيانات الـUAT البالغ عددها 500 مدرسة بقيت في المشروع السابق لتستمر كبيئة Staging/اختبار؛ لا تظهر في بيئة الإنتاج الجديدة. مشروع Staging الفارغ الآخر أُوقف مؤقتًا وقابل للاستئناف لتوفير المقعد المجاني اللازم للإنتاج.

المتبقي: نشر متغيرات Render الجديدة ثم تشغيل فحص الجاهزية والدخول والعزل الحي قبل إعلان الإغلاق النهائي.

### 2. البريد واستعادة كلمة المرور — أولوية عالية

مسار الاستعادة ورابط إعادة التوجيه الإنتاجي مضبوطان، لكن تسليم البريد يعتمد على SMTP/Supabase Auth Email Provider. لا يوجد في هذا الفحص دليل تسليم رسالة فعلية إلى صندوق بريد المستخدم.

الإغلاق المطلوب: اعتماد مزود SMTP، تنفيذ رسالة اختبار، والتحقق من وصولها ورابطها.

### 3. النسخ الاحتياطي والتعافي — أولوية حرجة

واجهة النسخ والتعافي تعمل بفشل آمن عند غياب موصل التخزين والجدولة؛ لا توجد نسخة إنتاجية موثقة ولا اختبار Restore معتمد.

الإغلاق المطلوب: مزود تخزين، جدول نسخ، سياسة احتفاظ، نسخة مشفرة، واختبار استعادة موثق قبل التسليم.

### 4. المراقبة والتنبيهات التشغيلية — مغلق جزئيًا

أضيفت مراقبة خارجية مجانية في GitHub Actions تفحص `/api/ready` كل خمس دقائق، مع خمس محاولات لتفادي إنذار الاستيقاظ البارد، وتفتح/تحدّث Issue عند الفشل وتغلقه عند التعافي. ما يزال تجميع السجلات والتنبيه إلى قناة مناوبة خارج GitHub غير موصولين.

### 5. DNS/SSL والنطاق التجاري — أولوية عالية

الخدمة متاحة حاليًا على نطاق Render. بيانات النطاق في الإدارة المركزية لا تثبت ملكية DNS أو إصدار SSL فعليًا.

الإغلاق المطلوب: نطاق تجاري، DNS، شهادة SSL، وفحص HTTP/DNS من موصل موثوق.

### 6. خطة تشغيل Render — قرار تجاري مطلوب

الخدمة الحالية على الخطة المجانية؛ قد يحدث إيقاف تلقائي وتأخير إقلاع يصل إلى نحو 50 ثانية، ولا تمثل هذه الخطة توافرًا تجاريًا مرتفعًا.

الإغلاق المطلوب: اعتماد خطة تشغيل مناسبة، أو قبول هذا القيد صراحةً ضمن اتفاق التسليم. لم يتم تفعيل اشتراك مدفوع دون موافقة مالية مستقلة.

### 7. اختبار عزل حي بحسابين مدرسيين مستقلين — أولوية عالية

اختبارات العزل البرمجية وUAT ناجحة، لكن شهادة التسليم النهائي تحتاج حسابي مدرسة مستقلين مع بياناتهما المعتمدة للتحقق من أن كل مدرسة ترى بياناتها فقط في الإنتاج.

الإغلاق المطلوب: تنفيذ اختبار دخول وقراءة/كتابة محدود بحسابين مستقلين، ثم حفظ دليل sanitized دون كلمات مرور أو رموز جلسات.

### 8. مخاطر الحزم — أولوية عالية قبل التسليم التجاري

فحص `npm audit --omit=dev` على قفل الحزم الحالي أظهر 12 إنذارًا معروفًا: 5 عالية، 5 متوسطة، و2 منخفضة، مع عدم توفر إصلاح آلي لبعضها. أبرزها `xlsx` المستخدم في استيراد/تصدير الجداول، وسلسلة `express-rate-limit`/`ip-address`.

الإغلاق المطلوب: تحديث أو استبدال الحزم المتأثرة بعد اختبار كامل، أو اعتماد استثناء أمني موثق مع تقييد صارم لحجم ونوع ووجهة الملفات قبل التسليم.

## قرار التسليم

لا يُنصح بتسليم تجاري نهائي قبل إغلاق البنود 1 و2 و3 و4 و8. التطبيق نفسه جاهز للمراجعة التشغيلية، لكن فصل البيئات والبريد والنسخ الاحتياطي والمراقبة ومخاطر الحزم هي شروط تسليم وليست تحسينات شكلية.

## قائمة اليوم السابق للتسليم

- [ ] اعتماد مشروع Supabase الإنتاجي وفصل Staging.
- [ ] تنظيف بيانات UAT بعد موافقة صريحة.
- [ ] اعتماد SMTP واختبار وصول رسالة استعادة كلمة المرور.
- [ ] تنفيذ نسخة احتياطية واستعادة تجريبية.
- [ ] تفعيل التنبيهات والمراقبة.
- [ ] ربط DNS/SSL والنطاق التجاري.
- [ ] تنفيذ اختبار عزل بحسابي مدرستين مستقلين.
- [ ] معالجة إنذارات الحزم أو اعتماد استثناء أمني موثق.
- [ ] حفظ أرقام الإصدارات ونتائج الفحص في محضر التسليم.
