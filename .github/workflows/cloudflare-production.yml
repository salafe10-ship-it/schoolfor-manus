name: Cloudflare production deploy

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cloudflare-production
  cancel-in-progress: true

jobs:
  deploy:
    name: Validate and deploy Cloudflare Worker
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      PRODUCTION_URL: ${{ vars.CLOUDFLARE_PRODUCTION_URL || 'https://schoolfor-manus.salafe10.workers.dev' }}
      EXPECTED_BUILD_COMMIT: ${{ github.sha }}
    steps:
      - name: Checkout exact production commit
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Require Cloudflare deployment credentials
        shell: bash
        run: |
          test -n "$CLOUDFLARE_API_TOKEN" || { echo 'Missing GitHub secret CLOUDFLARE_API_TOKEN'; exit 1; }
          test -n "$CLOUDFLARE_ACCOUNT_ID" || { echo 'Missing GitHub secret CLOUDFLARE_ACCOUNT_ID'; exit 1; }

      - name: Install dependencies
        run: npm ci

      - name: Validate source and tests
        run: |
          npm run route-integrity
          npm run lint
          npm test -- --run

      - name: Build release artifact
        run: npm run build
        env:
          BUILD_COMMIT_SHA: ${{ github.sha }}
          APP_VERSION: ${{ github.ref_name }}-${{ github.run_number }}
          BUILD_TIMESTAMP: ${{ github.event.head_commit.timestamp }}

      - name: Verify release artifact
        run: npm run verify:artifact

      - name: Deploy exact commit to Cloudflare
        run: |
          BUILD_VERSION="${GITHUB_REF_NAME}-${GITHUB_RUN_NUMBER}"
          BUILD_TIMESTAMP="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          npx wrangler deploy --config wrangler.jsonc \
            --keep-vars \
            --define "__EDUPRO_BUILD_VERSION__:\"${BUILD_VERSION}\"" \
            --define "__EDUPRO_BUILD_COMMIT__:\"${GITHUB_SHA}\"" \
            --define "__EDUPRO_BUILD_TIMESTAMP__:\"${BUILD_TIMESTAMP}\"" \
            --var "APP_VERSION:${BUILD_VERSION}" \
            --var "BUILD_COMMIT_SHA:${GITHUB_SHA}" \
            --var "BUILD_TIMESTAMP:${BUILD_TIMESTAMP}"

      - name: Verify live Cloudflare build identity and assets
        run: npm run smoke:production
env:
  VITE_FINANCIAL_WRITES_ENABLED: 'true'
